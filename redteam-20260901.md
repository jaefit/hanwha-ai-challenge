# 적대적 검증 — 모델·파이프라인·대시보드

작성일 2026-09-01 · 산출물: 자체 red team 1회차 (구현 전 진단서)
방어 배점: 문제 해결력 30점(논리적 원인 분석 15 · 데이터 근거 15) + AI 도구 활용 25점(Claude Code로 자체 red team 을 돌려 재현 가능한 근거를 뽑은 과정 자체가 산출물). T8 런북(`submission/runbook.md`)의 실패 대안 절 입력.

**공격자 관점으로 우리 것을 깬다.** 모든 항목은 `파일:줄` 또는 데이터 파일로 재현 가능한 근거를 붙였다. 추측은 "가설"로 표시한다.
검증 시각의 코드 기준: `1864118`. 테스트 26건 통과(`.venv/bin/python -m pytest tests -q`).

**검증 범위 — 전수 감사가 아니다.** 이번 패스가 읽은 것은 `src/nowcast.py` · `src/collector_api.py` · `src/publish.py` · `src/backtrack.py`(출구 계산부) · `run_all.sh` · `docs/index.html` · `docs/report.html` 과 `data/derived/` 산출물이다.
**읽지 않은 것: `src/collector_cctv.py` · `src/evaluate.py` · `src/exit_shares.py` · `src/feeder_leadlag.py` · `docs/report_easy.html` · `docs/deck.html`.**
이 중 `collector_cctv.py` 는 구역 밀도를 `lag_table()` 에 직접 먹이는 **미감사 입력 중 가장 큰 것**이다 — 다음 패스 1순위.

---

## 조치 현황 (2026-09-01 저녁 반영)

즉시 5건 + 높음 2건(H2·H5)을 TDD로 구현했다. 테스트 **26 → 41건**. 신규 15건 중 **13건은 red→green** 으로 실패를 먼저 확인했고, 2건(`show_end` 정상 입력 2종)은 기존 동작을 굳히는 회귀 가드라 처음부터 통과했다. T7 드라이런(9/2)이 이 조치들을 실제로 통과시키는지 검증한다.

| 항목 | 상태 | 검증 |
| :-- | :-- | :-- |
| C2 발행 신뢰성 | ✅ | `same_payload()` 가 `generated` 제외 비교 · `push()` 가 실패 시 `pull --rebase --autostash` 후 재시도 · 연속 실패 횟수를 `data/live/publish_fail_streak` 에 남기고 stderr 경보. 단위 테스트 1건 |
| C4 show_end 방어 | ✅ | `parse_show_end()` 정규식 + 범위(19:00~24:00). 인자 오류는 사용법 출력 후 종료(크래시 없음), 파일 형식 오류는 경고 + `source: planned_invalid_file` 로 드러남. 테스트 6건 |
| C5 API 오류 구분 | ✅ | `parse_body()` 가 비-JSON 응답의 원문 200자를 예외에 담는다(`INFO-100` 등). `redact()` 로 로그 기록 전 키 값 제거. 테스트 2건 |
| C3 수집기 감시 | ✅ | `run_all.sh` 워치독 — 30초마다 3개 프로세스 생존 확인·재기동, 수집 로그 10분 정체 시 경고, 시작 전 `.env` 필수 키 점검. 스텁 스모크로 재기동·트랩 동작 확인(자식 잔류 0) |
| L1 표 3-3 | ✅ | "표 3-3 — α 관측 채널(O1·O2)" 부여. KaTeX 재검증 통과(노드 59, 에러 0, 넘침 0) |
| H2 통제 문구 | ✅ | `CLOSED_NOTE` 로 시간대 분기. 실제 DOM 확인 — 20시 "공지 기준 20:40부터 통제 · 예년엔 조기 무정차 — 현장 확인" / 21시 "21:40 해제 예정 · 그전까지는 여의도역으로". 모델의 두 시간 무정차 근사는 유지하고 §3.3 에 안전측 근사임을 명시 |
| H5 커버리지 c | ⚠️ 부분 | 창을 14~17시로 넓혀 문서·코드 일치 · 산포 가드 추가((최대−최소)÷중앙값 > 0.3 이면 고정값, `coverage_spread` 로 발행) · 테스트 3건. **다만 클램프 (0.8, 3.0) 은 그대로 두었다** — 핫스팟 9역의 실제 구성을 측정한 적이 없어 더 좁은 상한을 뒷받침할 근거가 없다("출처 없는 수치를 쓰지 않는다"). 산포 가드가 현실적 실패(몇 창의 잡음)는 막지만, **c 가 일관되게 크게 나오는 경우의 α 감쇠는 남아 있다** |

**조치가 스스로 만든 실패 모드 2건도 같이 닫았다** — 고치기 전 코드에는 없던 위험이라 남겨두면 순손실이다.

| | 새로 생긴 위험 | 조치 |
| :-- | :-- | :-- |
| C2-a | `pull --rebase` 가 충돌하면 저장소가 rebase 진행 상태로 묶여 **이후 모든 틱의 커밋이 실패**한다 (두 기기가 같은 파일을 고쳤을 때) | 실패 시 `rebase --abort` 로 되돌리고 경보만 올린다. 자동 병합은 하지 않는다(사람이 판단할 일). 실제 충돌하는 로컬 저장소 2개를 만들어 검증하는 통합 테스트 1건 |
| C3-a | 수집기가 뜨자마자 죽는 크래시 루프면 30초마다 재기동 = **정상 주기의 10배로 쿼터를 태운다** | 10분 창 안에 재기동 5회를 넘으면 재시도 간격을 300초(정상 틱 주기)로 늘린다. 영구 정지는 안 한다 — 밤중에 조작자가 없을 수 있어 자가복구를 남긴다. 스텁 스모크로 확인(즉시 3회 → 경고 → 8초 간격 전환) |

**미조치로 남은 것:** C1(α 과신) · H1·H3·H4 · M1~M7. C1 은 프리즈 전 과제이고 나머지 판단은 §6.

### 추가 (2026-09-02)

**조치가 만든 실패 모드가 하나 더 있었다.** 위 C2-a·C3-a 와 같은 종류라 같이 적는다 — 자기 수정이 만든 위험은 원래 없던 것이라 남겨두면 순손실이다.

| | 새로 생긴 위험 | 조치 |
| :-- | :-- | :-- |
| 정규식-a | 9/1 혼잡장 작업 중 `docs/index.html` 의 `routeRisk` 를 `function routeRisk\(e\)\{[\s\S]*?\n\}\n` 로 교체했는데, `routeRisk` 가 한 줄 함수라 닫는 중괄호가 단독 줄이 아니었다. 패턴이 뒤를 계속 먹어 **`class RouteHeap` 과 `function routeAstar` 가 통째로 지워졌다.** `node --check` 는 통과했고(문법은 멀쩡) 화면도 지도까지는 정상이라 **네 커밋 동안 아무도 몰랐다.** 사용자가 경로 찾기를 눌러 `routeAstar is not defined` 를 보고 나서야 드러났다 | 두 블록을 `8d605a7^` 에서 인덱스 절단으로 복원(정규식 안 씀). 회귀 가드 `test_dashboard_has_no_undefined_local_functions` 추가 — 인라인 스크립트에서 호출되는 지역 함수가 전부 정의돼 있는지 본다. 블록을 지우면 `['routeAstar']` 로 실패하는 것을 역검증했다. 규칙은 `CLAUDE.md` 에 남겼다 |

**테스트 41 → 45건**(`.venv/bin/python -m pytest tests -q`, 2026-09-02 기준). 늘어난 4건: 위 회귀 가드 1건 + 혼잡장 `docs/app/field.js` 3건(`tests/field_spec.mjs` 를 node 로 돌려 커널 단조성·Cholesky 재구성·σ 순서·경험적 베이즈 사전 등 22개 단언을 확인한다. node 가 없으면 skip).

**감사 범위는 그대로 남아 있다.** 위 "검증 범위" 에 적은 미감사 파일 6종(`collector_cctv.py` 1순위)에 더해, 9/1 에 새로 들어온 **`docs/app/field.js` · `routing/` 경로탐색 · `tools/demo.sh` 는 아직 적대적 검증을 거치지 않았다.** 혼잡장은 공개 화면에서 가장 눈에 띄는 요소이므로 2회차 패스가 열리면 여기가 우선이다.

---

## 0. 먼저, 깨지지 않은 것

공격이 통하지 않은 지점도 기록한다 — 이후 세션이 같은 곳을 다시 파지 않게.

| 검사 | 방법 | 결과 |
| :-- | :-- | :-- |
| API 키 유출 | `.env` 9개 값을 git 추적 파일 전수 + `data/live/**` + `logs/*` 에서 문자열 검색 | 히트 0 |
| 에러 로그로 키 새는가 | 가짜 키로 실제 API 호출 → 예외 문자열 확인 | 키 미포함 (`JSONDecodeError`) |
| HTML 이스케이프 | `esc()` 정의(`docs/index.html:316`) · API 유래 문자열 17곳 전수 | 전부 적용됨 |
| 표 3-2·3-4·3-5·3-6 수치 | `exit_shares.json`·`backtest.json`·`lifepop_yeouido.json`·`eve_rehearsal.json` 에서 역산 | 전부 일치 |
| `SATBASE`(HTML 상수) vs 데이터 | 시간대별 6출구 합 대조 (20시 5,965 · 21시 4,347) | 일치 |
| KaTeX 렌더 | 1440·1024·900px DOM 계측 | 노드 55 = 소스 55, 에러 0, 넘침 0 |
| 스테일·오프라인 폴백 | latest.json 3일 경과 상태로 렌더 | 사전 예측표로 정상 강등, 상태 문구 정확 |
| 지도 실패 | 라이브러리·WebGL 차단 렌더 | 두 경우 모두 안내 문구로 폴백 |

---

## 1. 치명 — 9/5 당일 서비스가 조용히 무너지는 경로

### C1. α 사후분포가 과신한다. 관측 1건이 전체 예측을 2배 흔든다

**근거(실측).** 오늘 14:26 실제 산출물 `data/live/forecast_latest.json`:

```
관측 1건(하차 1·승차 0) → α p50 0.527 [0.425–0.645]
```

관측 **한 건**이 α를 1.0 → 0.527로 끌어내렸다. 사후 p90(0.645)이 사전 p10(0.73)보다도 낮다 — 사전분포가 사실상 아무 역할도 못 했다는 뜻이다.

**원인.** `src/nowcast.py:296` `assimilate()` 의 σ 가 **관측 오차만** 담는다.

```python
sigs = [max(rel * max(y, A + B) + ab, 1.0) for ...]   # rel = 0.15 / 0.20
```

`A`(2년 평균 증분)는 그 자체로 연도 간 편차가 크다 — 표 3-2 여의도(5) E는 38,281 ↔ 25,159, **±26%**. 예측항의 이 불확실성이 σ 에 없으므로 우도가 사전분포를 압도한다.

**결과가 더 나쁜 쪽.** 관측을 독립으로 곱하므로 밴드가 √n 으로 좁아진다. α=1.0 을 정확히 가리키는 이상적 관측만 넣고 개수를 늘려 측정했다:

| 관측 수 | 0 | 1 | 4 | 8 | 14 | 20 | 28 |
| :-- | --: | --: | --: | --: | --: | --: | --: |
| α 밴드 상대폭 | 65% | 33% | 23% | 17% | 13% | **11%** | 9% |

9/5 는 12~19시 O1 14창 + 19~23시 O2 8~10창 = **약 22~24건**. 화면에 뜰 밴드는 ±5% 수준이다. 그런데 **같은 모델의 교차연도 백테스트 오차는 0.420 / 0.438 (±42~44%)** 다. 대시보드가 실제 오차의 1/8 폭을 "예측 밴드"로 광고하게 된다.

30분 창 관측은 같은 군중을 연속으로 재는 것이라 독립이 아니다. 독립 가정이 이 축소의 직접 원인이다.

**실패 시나리오.** 9/5 13시, citydata 한 틱이 통제·집계 이상으로 튄 값을 준다 → α 0.5 로 확정 → 모든 출구 부하율이 절반으로 표시 → 밴드는 ±5% 라 "확신에 찬 오답" → 21시에 실제 인파가 2배로 나타나도 이후 관측이 앞선 20여 건에 묻혀 복구가 느리다.

---

### C2. 발행이 실패해도 아무도 모른다 (공개 대시보드 정지)

`src/publish.py:37-44`:

```python
if out.exists() and out.read_text(encoding="utf-8") == new:
    print("no change"); return
...
r = subprocess.run(["git", "commit", "-q", "-m", ...], cwd=ROOT)
if r.returncode == 0:
    subprocess.run(["git", "push", "-q", "origin", "main"], cwd=ROOT)
```

세 가지가 겹쳐 있다.

1. **"변경 없으면 커밋하지 않는다"는 문서 주장이 실제로는 항상 거짓이다.** `new` 안에 `generated` (= now())이 들어 있어 비교가 성립할 수 없다. 9/5 12~24시 = **약 144회 커밋·푸시**.
2. **`push` 실패를 확인하지 않는다.** 반환코드도 안 보고, 재시도도 없다.
3. 회사 PC나 다른 세션이 `main` 에 커밋을 하나라도 올려두면 push 는 non-fast-forward 로 거절된다. 그 뒤 **모든 틱이 로컬 커밋만 쌓고 조용히 실패**한다. 공개 페이지는 마지막 성공 시점에 얼어붙고, 로그를 열지 않는 한 알 수 없다.

**지금 이미 그 상태의 흔적이 있다.** 공개 중인 `docs/data/latest.json` 의 `generated` 는 **2026-08-29T10:28** — 3일 전이고, `assimilation: {}` 즉 T1c 데이터동화 이전 버전의 산출물이다.

---

### C3. 수집기가 죽으면 재시작도 경보도 없다

`run_all.sh:11-18` 은 세 프로세스를 백그라운드로 띄우고 `wait` 한다. `collector_api.py` 가 죽어도 `run_all.sh` 는 나머지를 기다리며 계속 살아 있다. 화면에는 아무 변화가 없고, 대시보드만 "오래됨"으로 서서히 미끄러진다. 헬스체크·재시작·알림이 전부 없다.

`.env` 가 없거나 키 이름이 하나 틀리면 `src/collector_api.py:26` 의 `ENV["SEOUL_KEY_GENERAL"]` 가 임포트 시점에 `KeyError` 로 죽는다. 회사 PC ↔ 맥을 오가는 조건에서 현실적인 시나리오다(`next_todos`: SUBWAY2~6은 회사 PC `.env` 에 아직 없음).

---

### C4. 쇼 종료 시각 기입 — 세 가지 방식이 전부 취약한데 담당자가 미정이다

`src/nowcast.py:62-70`. 실제로 돌려 본 결과:

| 입력 | 결과 |
| :-- | :-- |
| `--show-end` (값 누락) | **크래시** `IndexError: list index out of range` |
| `--show-end 2110` | **크래시** `ValueError: not enough values to unpack` |
| `show_end.txt` = `2110` | **조용히 무시** → `planned` 21:10 으로 진행 |
| `show_end.txt` = `21시10분` | **조용히 무시** → `planned` |
| `show_end.txt` = `25:99` | **통과** → 유출 곡선 **369분(6시간) 이동**, 예측 전파 |

콜론을 빼먹으면 아무 일도 안 일어난 것처럼 보이고(모델은 계획값을 쓴다), 오타로 앞자리를 잘못 치면 곡선이 통째로 밀린다. 범위 검증이 없다. 이 값은 오차 예산 ⑤ 항목인데 `next_todos` 상 **담당 1명이 아직 미정**이다.

---

### C5. API 실패 원인이 로그에서 구분되지 않는다

서울 열린데이터광장은 인증 실패·쿼터 초과에 **HTTP 200 + XML** 을 준다. 실제 확인:

```
HTTP 200
<RESULT><CODE>INFO-100</CODE><MESSAGE><![CDATA[인증키가 유효하지 않습니다. ...]]></MESSAGE></RESULT>
```

`src/collector_api.py:53-55` 는 `json.loads` 를 바로 걸므로 예외가 `JSONDecodeError: Expecting value: line 1 column 1 (char 0)` 이 되고, `:128` 이 그 문자열만 기록한다. **쿼터 초과·키 오타·핫스팟 이름 오류·API 장애가 로그에서 전부 같은 한 줄로 보인다.** 정작 원인을 적어 보낸 `MESSAGE` 는 버려진다. 당일 20분 안에 원인을 찾아야 하는 상황에서 치명적이다.

---

## 2. 높음 — 결론의 신뢰도를 직접 깎는 것

### H1. 백테스트가 수요의 55%만 검증한다

`data/derived/backtest.json` 의 `stations` 는 4개 — 여의도(5)·여의나루(5)·신길(1·5)·마포역 도보. `exit_shares.json` `share_mean` 으로 환산하면 **검증된 비중 55.4%**, **미검증 44.6%**(9호선 여의도 16.6 + 샛강 17.1 + 국회 10.9)다. 9호선은 OA-12921 에 없어 시간대 대조가 불가능하기 때문이다.

문제는 **미검증 구간에 최악 후보가 몰려 있다**는 점이다. 사전 예측표 22시 부하율 상위는 여의나루 0.91 · **샛강 0.81** · 여의도(5) 0.63 · **국회 0.61** — 2·4위가 검증 밖이고, 그 용량은 전부 `estimated_capacity: true`(교통카드 승차비 비례 추정)다. 표 3-4 는 이 사실을 표 아래 문장으로만 언급한다.

**심사위원의 한 문장 공격:** "샛강이 두 번째로 위험하다는 근거는 무엇으로 검증했나?" — 현재 답은 "검증 못 했다"이다.

### H2. 2026 통제창을 두 시간으로 반올림해 60분의 운행을 버린다

공식 공지는 **20:40~21:40**. 모델은 `src/nowcast.py:57` 에서 20시·21시 **두 시간 전체**를 무정차로 둔다.

```python
CLOSED = {("여의나루(5)", 20), ("여의나루(5)", 21)}
```

실제로 열려 있는 20:00~20:40(40분)과 21:40~22:00(20분), 합 60분의 처리 용량이 사라지고 그 수요가 전부 22시로 이월된다. 22시 여의나루가 최악(0.91)이라는 우리 대표 결론이 이 근사에 직접 의존한다.

**사용자에게 틀린 지시가 나간다.** `docs/index.html:349` 의 통제 행 문구는 고정 문자열이다:

> 무정차 통과 · 통제 20:40~21:40 · 여의도역으로

20시 탭에서도 이 문구가 뜬다. 20:10에 여의나루 앞에 선 사람은 실제로 탈 수 있는데 **1.5km 떨어진 여의도역으로 보내진다.**

**다만 반대 방향 위험이 더 크다.** 2024·2025 실적은 **19시부터** 하차가 0 이었다 — 현장은 공지보다 일찍 무정차로 전환했다. "20:40까지 정차한다"고 단정했다가 실제로 조기 무정차면, 사용자는 **탈 수 없는 역에서 대기**하게 된다. 헛걸음 1.5km 보다 나쁜 결과다. 따라서 모델은 보수적 근사를 유지하고, **문구만 단정에서 조건부로 바꾸는 것이 옳은 방향**이다(§5 참조).

### H3. 19시 여의나루 예측은 검증 근거가 0인데 낙관적이다

`backtest.json` 의 `closed_by_year` 는 2024·2025 모두 `[19, 20, 21]` — 두 해 다 19시가 통제였다. 2026 모델은 19시를 **개방**으로 둔다(부하율 0.07). 즉 백테스트가 한 번도 검증하지 못한 상태다.

방향이 엇갈린다는 게 핵심이다. **20·21시는 실제보다 보수적(다 닫음)인데 19시는 실제 이력보다 낙관적(엶)이다.** 안전측 가정이라는 설명이 두 곳에 동시에 성립하지 않는다.

### H4. 사전 예측표가 "평균의 함정"에 빠졌다 — 화면에 설명 불가능한 조합이 뜬다

`src/backtrack.py:283` 은 연도별로 계산한 부하율·대기시간을 **각각 평균**한다. 여의나루 22시:

```
load 0.91  (2024 0.628 · 2025 1.186)   wait_min 6
```

부하율 0.91 이면 정의상 대기는 0분이다(`max(0, 수요−용량)`). 6분은 2025년의 11분과 2024년의 0분을 평균한 값이다. 사용자는 **"0.91 · 대기 ~6분(추정)"** 이라는 자기모순 조합을 본다.

더 중요한 건 감춰지는 정보다. 실제 사실은 "2년 중 1년은 용량을 19% 넘겼다"인데, 헤드라인 0.91은 "안 넘쳤다"로 읽힌다. `load_lo/hi` 0.57–1.30이 그 정보를 담고 있지만 작은 글씨다.

### H5. 커버리지 c 하나가 α를 최대 3배 흔든다

`src/nowcast.py:50`:

```python
COVERAGE_FALLBACK, COVERAGE_CLAMP = 1.3, (0.8, 3.0)
```

O2 예측항은 `A = c·초과/2`, `B = c·평시/2` 이므로 α 는 대략 c 에 반비례한다. 클램프 폭이 0.8~3.0 이라 **c 하나로 α 가 3.75배 범위를 움직인다.** 채택 조건은 표본 3창 이상(`len(cs) >= 3`)뿐이고, 표본 자체의 산포는 보지 않는다.

**문서–코드 불일치도 있다.** 주석(`:50`)·docstring(`:239`)·보고서 §3.4는 "당일 14~17시"라고 쓰는데 코드(`:251`)는 `if 14 <= h <= 16` — 14·15·16시만이다.

---

## 3. 중간 — 제품 논리와 UX

### M1. 순위가 부하율만 본다 — 도달 시간이 랭킹에 없다

21시 탭 1·2위는 **신길(도보 58분)** 과 **마포역 도보(43분)** 다. 21시에 출발하면 22시에 도착하는데, 화면이 보여준 0.14·0.24 는 **21시 기준값**이다.

모순은 모델 안에 있다. `lag_table()` 로 계산한 도달 지연을 **수요 배분에는 쓰면서**(`compute_exits`), **사용자 랭킹에는 쓰지 않는다**(`docs/index.html:338-341` 은 선택한 시각의 `load` 로만 정렬). 우리가 가진 정보를 사용자에게만 안 주고 있다.

### M2. 데이터가 비면 "여의나루 통제"로 표시된다

`docs/index.html:337` 이 `closed: !!c.closed || c.load == null` 로 판정하고, `:349` 의 통제 행은 여의나루 전용 문구를 고정으로 쓴다. 어떤 출구든 `load` 가 없으면 **"무정차 통과 · 통제 20:40~21:40 · 여의도역으로"** 로 렌더된다. 결손을 통제로 오표시한다.

### M3. CCTV 패널만 스테일 가드를 안 받는다

`:472-473` 의 12시간 스테일 스왑은 `d.forecast` 만 사전 예측표로 바꾸고, `d.cctv` 는 그대로 `cams()` 에 넘어간다(`:424`). 지금 공개 페이지의 CCTV 카드는 **8/29 10:28 판독값**을 현재값처럼 보여준다 — 구분은 작은 글씨의 시각 표기뿐이다.

**(2026-09-01 저녁 해결)** 혼잡장을 GP 회귀로 바꾸면서 **스테일을 하드컷이 아니라 노이즈 감쇠로** 처리했다 — 관측이 오래될수록 σ 가 커져(15분 √2배, 2시간 8배) 사후가 자연스럽게 사전으로 회귀한다. 실증: 8/31 로그(1,700분 전)를 넣으면 장이 정확히 평탄해지고 불확실성이 최대가 된다. `publish.py` 가 `confidence·flags·density·calibrated` 를 발행하게 되어 오탐도 σ 확대로 눌린다. **다만 `cams()` 의 CCTV 카드와 `d.forecast` 스왑은 아직 그대로다** — 카드 쪽 M3 는 미해결.

**(원 기록) APF 혼잡장이 이 구멍을 그대로 물려받았다.** 팀원 핸드오프로 들어온 `apfRender()` 는 같은 `d.cctv` 를 먹는다. 즉 **8/29 판독값이 "지금의 혼잡장"으로 그려지고**, 그 위에서 경로의 혼잡 회피 비용까지 계산된다. 스테일 가드를 `d.cctv` 로 넓히면 APF·경로가 함께 고쳐진다 — 한 곳을 고치면 세 화면이 낫는다. 필터도 다르다: `apfRows()` 는 `ok!==false` 만 보는데 `nowcast.zone_density_from_cctv()` 는 `confidence=="ok"` + `count>=20` 까지 본다. 미보정 카메라 한 대가 혼잡장을 끌 수 있다.

### M4. 모든 시각 판단이 뷰어 기기 시계에 달려 있다

`pick()`(`:326`)은 `new Date().getHours()` 로 기본 탭을 고르고, 신선도 `age`(`:474`)도 기기 시계로 잰다. 시계가 어긋난 폰이나 해외 로밍 단말에서는 엉뚱한 시간 탭이 열리고, 12시간 이상 어긋나면 **사전 예측표로 강등**된다. 국제 행사이므로 실제로 발생할 수 있는 조건이다.

### M5. 타일 서버만 죽으면 폴백이 없다

라이브러리 로드 실패(`:435`)와 WebGL 부재(`:433`)는 안내 문구로 처리된다 — 확인했다. 하지만 `maplibre-gl.js` 는 **unpkg**, 타일은 **tiles.openfreemap.org**(무보증 커뮤니티 서비스) 단일 의존이다. 스크립트는 살아 있고 타일만 실패하면 **빈 배경에 마커만 뜬 지도**가 되고, 이 경우를 잡는 코드가 없다. 100만 명 규모 현장의 통신 포화가 정확히 이 조건이다.

### M6. 사전 모드에서 α=1.00 을 측정값처럼 보여준다

`:352` 는 α 를 큰 숫자 타일로 띄우고 라벨은 "오늘 유입 ÷ 2년 평균"이다. 사전 예측 모드의 1.00 은 **관측이 없어서 기본값**인데, 화면은 "오늘 재보니 예년과 같더라"로 읽힌다. 같은 줄의 실시간 인구 타일은 값이 없을 때 `·` 만 남는다(`:355`).

### M7. 수집은 하지만 화면에 없는 신호

`nowcast.py` 가 내보내는 `assimilation.edge_hit`(사후분포가 격자 끝에 붙음 = 관측과 사전이 크게 어긋남)과 `alerts_live`(사고·통제 메시지 최대 10건)를 UI가 전혀 쓰지 않는다. 전자는 모델 자신의 이상 경보이고 후자는 사용자에게 가장 실시간성 높은 정보다.

---

## 4. 낮음

### L1. 표 3-3 이 없다

`docs/report.html` 의 표 번호가 3-2 → 3-4 로 건너뛴다. §3.4 의 O1/O2 관측 채널 표(`:257`)가 번호를 못 받았다. 표 3-4 는 `id="tbl-3-4"` 로 참조되고 있으므로 **O1/O2 표에 3-3 을 부여**하는 쪽이 안전하다.

---

## 5. 해결안 — 분류별

**분류 기준:** 즉시 = 오늘·내일 · 프리즈 전 = 9/4 12:00 이전 · 제출물 = 코드 안 건드리고 문서·발표로 방어 · 이후 = 제출 후.

### 즉시 (T7 드라이런 9/2 전에)

| # | 조치 | 내용 |
| :-- | :-- | :-- |
| C2 | `publish.py` 발행 신뢰성 | ① `generated` 를 뺀 본문으로 비교해 무변경 시 커밋 생략 ② `push` 반환코드 확인 → 실패 시 `git pull --rebase` 1회 후 재시도 ③ 연속 2회 실패면 stderr 에 눈에 띄는 경보 + `logs/publish.log` 에 마커 |
| C4 | show_end 입력 방어 | `--show-end` 값 누락·형식 오류를 잡아 사용법 출력 후 종료(크래시 금지) · `show_end.txt` 는 `HH:MM` 정규식 + 범위(19~24시) 검증, 형식 불일치 시 **경고를 찍고** planned 폴백 · 파싱 결과를 로그 1줄로 확인 출력 |
| C5 | API 오류 구분 | `get()` 에서 본문이 JSON 이 아니면 앞 200자를 그대로 예외 메시지에 담는다. 실측 확인한 `INFO-100`(인증키 무효)처럼 쿼터 초과·장소명 오류도 각자의 `CODE`/`MESSAGE` 로 로그에 남아 당일 원인 판별 가능 |
| C3 | 수집기 감시 | `run_all.sh` 에 워치독 루프 추가 — 30초마다 P1·P2 생존 확인, 죽었으면 재기동 + `logs/watchdog.log`. 최소한 `data/live/api_*.jsonl` 의 mtime 이 10분 넘게 안 변하면 터미널 벨 |
| L1 | 표 3-3 | §3.4 O1/O2 표에 "표 3-3 — α 관측 채널" 부여 |

### 프리즈 전 (9/4 12:00)

| # | 조치 | 내용 |
| :-- | :-- | :-- |
| C1 | α 밴드를 정직하게 | (a) σ 에 **예측항 자체의 연도 간 편차**를 더한다 — `σ = sqrt((rel·pred)² + (model_rel·pred)²)`, `model_rel` 은 백테스트 B 오차(0.42)에서 가져온다. (b) 관측 자기상관 보정 — 같은 채널 연속 창은 유효표본을 줄여 센다(예: 채널당 유효 n = √실제 n). (c) **밴드 하한을 백테스트 B 오차 아래로 내려가지 못하게 클램프**한다. 셋 중 (c)만이라도 하면 "±5% 확신"은 막힌다 |
| H2 | 통제창 — **문구만 고친다** | 모델의 두 시간 무정차 근사는 **유지**한다(예년 조기 무정차 이력이 있어 부분 개방 용량을 주는 쪽이 더 위험하다). 대신 UI 통제 행 문구를 시간대별로 분기하되 단정하지 않는다 — 20시 "**공지 기준 20:40부터 통제 · 예년엔 조기 무정차 — 현장 확인**", 21시 "**21:40 해제 예정 · 이후 정차**". §3.3 에 "두 시간 무정차는 안전측 근사"임을 명시 |
| M2 | 결손 ≠ 통제 | `closed` 판정에서 `c.load == null` 을 분리해 "데이터 없음" 행으로 별도 렌더 |
| M6 | α 타일 라벨 | 사전 모드에서는 α 타일을 "—" 또는 "사전값 1.00(관측 전)" 으로. 빈 실시간 인구 타일은 `—` 로 채운다 |
| M7 | 신호 노출 | `edge_hit` 참이면 헤더에 "관측이 예상 범위를 벗어남 — 사전 예측 병행" 배지. `alerts_live` 를 "오늘의 통제" 블록 아래 리스트로 (이미 `esc()` 경로 있음) |
| M3 | CCTV 스테일 | `cams()` 에서 각 카드의 `ts` 가 30분 이상 지났으면 흐리게 + "옛 판독" 표기 |
| H5 | 커버리지 c | 창을 14~17시로 넓혀 코드·문서 일치. 표본 산포가 크면(변동계수 > 0.3) fallback 사용. 클램프를 (1.0, 2.0)으로 좁힌다 |
| C3 | 런북 항목 | `.env` 키 9개 존재 확인을 시작 전 체크리스트 1행으로 (`--budget` 이 이미 있으니 그 앞에 키 유무 검사 추가) |

### 제출물로 방어 (코드 수정 없음)

| # | 조치 |
| :-- | :-- |
| H1 | 표 3-4 에 **"검증 커버리지 55.4% / 미검증 44.6%"** 를 명시 행으로 넣고, 9호선 3역이 미검증인 이유(OA-12921 에 9호선 없음)와 그 대안(교통카드 일별 비례 추정)을 한 문단. **약점을 먼저 말하는 쪽이 심사에서 강하다** — 지금은 각주로만 있어 "숨겼다"로 읽힐 여지가 있다 |
| H3 | 19시 여의나루가 미검증 구간임을 §3.5 에 한 줄. 보수/낙관 비대칭을 인정하고 "2026 공지 기준을 따랐다"로 근거를 명시 |
| H4 | 사전 예측표의 headline 이 연도 평균임을 밝히고, 여의나루 22시는 **"2024 0.63 / 2025 1.19 — 2년 중 1년은 용량 초과"** 를 본문 문장으로 승격 |
| M1 | 도달 시간이 랭킹에 없다는 점을 한계로 명시 (아래 '이후' 항목의 예고와 함께) |
| C1 | 밴드의 의미를 "α 규모 불확실성만 담는다. 시간 배분·연도 이전·용량 추정 오차는 미포함"으로 §3.4·§3.5 에 명시 |

### 이후 (제출 후 / 조건부)

- **M1 도착시각 기준 랭킹**: 선택 시각 대신 "지금 출발 → 도달 시각"의 부하율로 정렬. `lag_table()` 결과가 이미 `forecast_latest.json` 에 실려 있으므로 UI만 고치면 된다. 제품 가치가 가장 크게 오르는 한 수지만 프리즈 전에 넣기엔 검증 시간이 부족하다 — **9/6 이후 개선안으로 발표에 넣는 편이 낫다**
- **M5 타일 폴백**: 타일 로드 실패 감지(`map.on('error')`) → 정적 SVG 약도로 전환. 또는 여의도 일대 타일을 사전 캐시
- **M4 시각 기준**: `generated` 시각을 서버 기준으로 삼아 탭·신선도 계산. 기기 시계와 5분 이상 어긋나면 안내
- **C2 이중화**: GitHub Actions 백업 수집기(백로그 기존 항목) — 맥이 죽어도 발행이 이어진다

---

## 6. 판단이 필요한 열린 질문

1. **C1 (c) 밴드 클램프**를 넣으면 화면 밴드가 지금보다 훨씬 넓어진다. "정직하지만 덜 인상적"과 "좁지만 방어 불가" 중 어느 쪽을 택할지 — 심사 기준(데이터 근거 15점)에서는 전자가 유리하다고 본다
2. **H2** 는 위에서 "문구만 고친다"로 결론냈지만, 근본 해결(부분 개방 용량)을 완전히 포기할지는 판단이 필요하다. 부분 개방을 넣으면 22시 여의나루 최악이라는 대표 결론의 **크기가 바뀌고 백테스트를 다시 돌려야 한다** — 프리즈까지 시간이 빠듯하다. 제안: 프리즈 전에는 손대지 않고, 9/5 현장에서 **실제 무정차 전환 시각을 기록**해 9/6 검증 재료로 삼는다(런북 항목 추가)
3. **전야제 합격선 1.9배 미달 시의 행동이 정의되어 있지 않다.** 2026 전야에 행사가 없거나 우천이면 배수가 안 나올 수 있다 — 그때 "파이프라인 고장"으로 오판하지 않으려면 합격선을 단일 임계값이 아니라 "여의나루 하차가 평시 금요일을 유의하게 초과하는가"로 두는 편이 안전하다 (가설: 2024·2025 상승분이 전야 행사 때문인지 금요일 한강공원 효과인지 분리되지 않았다)
