# 이 저장소의 작업 규칙

한화 신입사원 AI 실습 과제 03 「고객 페인포인트 해결」 작업 공간이다.
전체 맥락은 `README.md` 와 `BRIEF.md` 를 먼저 읽어라.

## 이 과제의 승부처

평가표 100점 중 **AI 도구 활용 축이 50점**이다. 기획서만 내면 절반을 통째로 놓친다.
어떤 도구를 왜 골랐고 그걸로 실제 무엇을 뽑았는지가 핵심이고, 실물 산출물이 반드시 있어야 한다.
나머지는 문제 해결력 30점(논리적 원인 분석 15 + 데이터 근거 15), 현업 적용 가능성 20점이다.
새 문서를 쓸 때는 그 문서가 어느 배점을 방어하는지 명시해라.

## 숫자를 다루는 규칙

이 프로젝트는 "가상 데이터 대신 공시 실데이터를 쓴다"를 차별점으로 잡았다. 그래서:

1. **출처 없는 수치를 쓰지 않는다.** 모든 숫자에는 출처와 기준일이 붙어야 한다.
2. **모르는 값은 비워둔다.** 추정해서 채우지 않는다.
   실제로 `deliverable-etf-cards.md` 의 "최근 비중 변동" 항목은 과거 구성내역이 없어 비워둔 상태다.
3. 기존 문서의 수치를 인용할 때는 그 문서에 적힌 기준일을 함께 옮긴다.

## 문서 관례

- 모든 문서는 한국어로 쓴다.
- 문서 상단에 작성일과 무엇의 산출물인지를 적는다.
- 판단이 바뀌면 이전 서술을 지우지 말고 정정 표시와 날짜를 남긴다.
  `BRIEF.md` 의 "방산도 03 대상이 된다 (2026-08-27 정정)" 이 그 형식이다.
- 확정되지 않은 것은 "미확정" 절에 체크박스로 모아둔다. 추측으로 메우지 않는다.

## 아직 확정되지 않은 것

**다룰 페인포인트가 아직 안 정해졌다.** 후보 20개를 세 차례에 걸쳐 돌려본 상태이고,
그중 F3(ETF 구성 불투명)만 산출물 시제품까지 만들어봤다. 시제품이 있다고 확정된 것이 아니다 —
`painpoint-analysis.md` PART 5 의 최신 순위표에서 F3 는 4위다. F3 를 전제로 글을 쓰지 마라.

그 밖에 마감일, 제출 분량·형식, 개인 과제인지 팀 과제인지, 설문 1,000건을 제공받는지 직접
생성하는지가 미정이다. 정해지면 `BRIEF.md` 의 미확정 목록과 `spec-f3-etf.md` 의 일정 절을
함께 갱신해라.

## 건드리지 말 것

`assignment/` 는 배포받은 원본이다. 편집하지 않는다. 판독 결과는 `BRIEF.md` 에 쓴다.
