# 이 저장소의 작업 규칙

한화 신입사원 AI 실습 과제 03 「고객 페인포인트 해결」 작업 공간이다.
전체 맥락은 `README.md` 와 `BRIEF.md` 를 먼저 읽고, **현재 작업 기준은 `topic-fireworks.md`** 다.

## 확정된 것 (2026-08-29)

- **주제: 서울세계불꽃축제(9/5) 관람객 혼잡·귀가 내비.** **제품명 「불꽃배웅」, 한 줄 「눈부신 밤의 끝, 집으로 가는 길까지.」(2026-09-05 결정)** — 덱·README·보고서엔 반영, `go.html`·`index.html` 은 최종 동결 뒤라 미반영(문구 hotfix 는 사용자 결정). 서울시 실시간 도시데이터 위에 결정 레이어(위치×목적지×시각 → 출구 랭킹·대기·오프라인 플랜) + TOPIS 공개 CCTV 밀도 분석 + KT 생활이동 OD 기반 예측.
- **마감 9/8(화) 22:00.** 오프라인 피드백 9/4(금) 오전. 제출 = 웹 편집기 9항목 + 영상 3분 + 웹페이지 링크(또는 파일 ≤50MB).
- 과정명이 "AI 업무자동화 과정"이고 교재 도구가 Codex CLI·Claude Code다. **AI 도구 25점은 "Claude Code로 무엇을 자동화했나"로 채운다.** 프롬프트·명령·실패·패치를 기록해라.
- 방산·금융 후보 20개(`painpoint-analysis.md`)와 F3 스펙·카드는 **보류**. 삭제하지 않는다.

## 코드·검증 규칙 (2026-09-02 추가)

- **회귀 테스트가 기준선이다.** 변경 후 `.venv/bin/python -m pytest tests -q` 통과(9/3 기준 74건). 새 동작은 TDD — 실패를 먼저 확인하고 구현한다.
- **코드 동결 2단계** (2026-09-03 변경 — 전엔 9/4 12:00 단일). ① **9/4 17:00 기능 동결** — 새 기능 중단, 전야제 리허설 직전. ② **9/5 10:00 최종 동결** — 전야제에서 나온 것만 반영하고 테스트 통과 후 push. 미룬 이유: 전야제로 확인하기로 한 항목(M8 일몰·정지 군중, API 시차 30분 가설)이 12:00 동결이면 발견해도 hotfix 로만 들어간다. 두 동결 이후 `src/`·`docs/index.html` 변경은 hotfix만, 테스트 통과 후 push. **예외(2026-09-04 사용자 결정): `docs/go.html` v2** — Claude Design 「귀가 내비 v2」 이식을 9/4 17:00 이전에 넣었다. 전야제 리허설에서 실기기로 확인할 것(지도·혼잡장·위치 권한·핀 드래그). 관람객 화면 결정 2건: ① 1위 = 도보+대기 시간(부하율 순위는 운영 화면만) ② 경로 = 운영 화면과 같은 보행망 A*(첫 화면 뒤 수신, 받기 전엔 근사 경유점 + 「근사」 표시). 근사만 쓰자던 같은 날 오전 결정은 "직선은 우회를 못 한다"로 되돌렸다.
- **결함 대장은 `redteam-20260901.md`.** 발견한 결함은 여기 등급(치명/높음/중간/낮음)과 `파일:줄` 근거를 붙여 기록하고, 조치하면 "조치 현황" 표에 반영한다. 다른 문서에 복제하지 않는다.
- **여러 줄 블록을 정규식으로 갈아끼우지 않는다.** `docs/index.html` 처럼 함수가 붙어 있는 파일에서 `function f\(\)\{[\s\S]*?\n\}` 류는 옆 함수까지 삼킨다(9/1 `routeAstar` 유실). 정확 문자열 1회 치환이나 인덱스 절단을 쓰고, 교체 전후로 정의 개수를 센다. 문법 검사만으로는 안 잡힌다.
- `docs/app/field.js` 는 **DOM 을 만지지 않는 순수 함수**로 유지한다 — 그래야 `tests/field_spec.mjs` 가 배포 중인 파일을 node 로 그대로 검증할 수 있다.

## 이 과제의 승부처

평가표 100점 중 **AI 도구 활용 축이 50점**이다. 기획서만 내면 절반을 통째로 놓친다.
어떤 도구를 왜 골랐고 그걸로 실제 무엇을 뽑았는지가 핵심이고, 실물 산출물이 반드시 있어야 한다.
나머지는 문제 해결력 30점(논리적 원인 분석 15 + 데이터 근거 15), 현업 적용 가능성 20점이다.
새 문서를 쓸 때는 그 문서가 어느 배점을 방어하는지 명시해라.

## 숫자를 다루는 규칙

1. **출처 없는 수치를 쓰지 않는다.** 모든 숫자에는 출처와 기준일이 붙어야 한다.
2. **모르는 값은 비워둔다.** 추정해서 채우지 않는다. 추정이 불가피하면 "추정"이라고 쓰고 방법을 적는다.
3. 기존 문서의 수치를 인용할 때는 그 문서에 적힌 기준일을 함께 옮긴다.
4. KT 생활이동 `cnt`는 추정치(소수)다. 절대값이 아니라 비율·형태로 쓴다.

## 비밀·데이터 규칙

- **API 키, 팀 수정 PIN, 수업 제출코드는 어떤 파일에도 쓰지 않는다.** 키는 `.env`(git 제외)에만. 문서에는 "일반키/지하철키"라고만 적는다.
- 원본 대용량 데이터는 `data/raw/`(git 제외). 커밋은 `data/derived/`의 요약본만. 재다운로드는 `src/fetch_seoul_data.py`.
- CCTV는 프레임을 저장하지 않는다. 집계값(인원·점유율·흐름)만 저장한다.
- 배포받은 원본 과제 자료(PDF·PNG)는 사내 자료다. **저장소에 넣지 않는다**(로컬 `../05_ai_challenge_assignment_local/`). 이 저장소는 2026-08-29부터 **public**이며 대시보드는 `docs/`에서 GitHub Pages로 낸다.

## 문서 관례

- 모든 문서는 한국어로 쓴다. 상단에 작성일과 무엇의 산출물인지 적는다.
- 판단이 바뀌면 이전 서술을 지우지 말고 정정 표시와 날짜를 남긴다. `BRIEF.md`의 "(2026-08-27 정정)" 형식.
- 확정되지 않은 것은 "미확정" 절에 체크박스로 모아둔다. 추측으로 메우지 않는다.
- 회사 PC와 맥을 오가며 작업한다. **작업 후 commit + push.**

## 아직 확정되지 않은 것

`topic-fireworks.md` §10 이 기준이다: 팀·파트 구성, 수단 코드표, 9호선 시간대 실적, 대용량 원본 공유 방식(Release 자산 예정).

**기능 동결(9/4 17:00) 전에 사람이 정해야 하는 것 2건** (2026-09-02):

1. **C1 α 밴드 클램프** — 사후 밴드가 관측 20건이면 ±5%로 좁아지는데 백테스트 cross-year 오차는 ±42%다. 밴드에 모델 오차를 넣을지. 근거 `redteam-20260901.md` §C1
2. **전야제 합격선의 의미** — "여의나루 하차 = 평시 금요일의 1.9배"를 리허설 정상신호로 쓰는데, 미달했을 때 무엇을 할지가 정의돼 있지 않다
