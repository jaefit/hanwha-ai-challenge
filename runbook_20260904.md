# 운전 런북 — 9/4(금) 17:08 ~ 9/6(일) 00:00 무중단

작성 2026-09-04 17:10. 플랜 T8(9/4 전야제 리허설 런북)의 산출물이자 9/5 본번 운전 절차. 맥 한 대에서 `run_all.sh` 를 **끄지 않고** 31시간 돌린다.
방어하는 배점: 문제 해결력(데이터 근거 15) — 전야제·당일 실측이 있어야 오차표(T6)가 나온다.

- 시작: **2026-09-04 17:08:45** `nohup ./run_all.sh > logs/run_20260904.log 2>&1 &` — 부모 pid **54527**(ppid 1, 터미널과 분리됨), api 54533 · cctv 54534 · nowcast/publish 54535 · caffeinate 54532
- 종료: **2026-09-06 00:00** `kill -INT 54527` (트랩이 자식·caffeinate 까지 정리) → `./tools/status.sh` 로 프로세스 0 확인
- 매 시각 점검: `./tools/status.sh` 한 번. 정상 기준 = 전원 AC · 프로세스 전부 1 · `api_last_ok` 10분 이내 · 발행 실패 0 · 시간 공백 없음 · CCTV ok ≥ 18/23

## 0. 지금 당장 (17:10)

| # | 할 것 | 왜 |
| :-- | :-- | :-- |
| 1 | **AC 전원 연결** — 시작 시점 배터리 80% 방전 중이었다 | C6: 배터리에서 뚜껑 덮으면 자고, `caffeinate` 가 못 막는다. 6시간이면 새벽에 꺼진다 |
| 2 | **뚜껑 열어 둠.** 자리 비울 땐 밝기 최저, 덮지 않는다 | 같은 이유. `Ctrl+Shift+전원` 도 쓰지 않는다 |
| 3 | 자동 업데이트·재시동 없음 확인(시스템 설정 → 일반 → 소프트웨어 업데이트 → 자동 끔) | 야간 재시동이면 수집이 통째로 빈다 |
| 4 | `./tools/status.sh` — 전원이 `AC Power` 로 바뀌었는지 | 위 1 확인 |

## 1. 시간표

### 9/4(금) 전야제 — 리허설 수집

| 시각 | 확인 | 정상이면 | 아니면 |
| :-- | :-- | :-- | :-- |
| 17:15 | `status.sh` — `degraded=[]` 로 돌아왔나(17:08 첫 발행은 수집기와 동시에 떠서 `['citydata']` 였다) | 다음 틱 17:13 발행에서 비어 있음 | 17:20 에도 남으면 §3-② |
| 17:30 | Pages go.html 필 「실시간 HH:MM」· 운영 화면 상단 상태 | 두 화면 같은 시각 | 「수집 끊김」이면 §3-② |
| 18:00 | 지하철 실시간 도착 수집 시작(17~23시, 키 6개 회전) — `logs/api.log` 에 `trains` 줄 | 5분마다 역별 줄 | 한 키만 계속 오류면 `.env` 순서 확인 |
| 18:50 | **일몰 — M8 관측.** `status.sh` 의 CCTV ok 수와 `latest.json` `cctv.*.flags` 에 `bg_fail` 이 몇 대에 붙는지 적어 둔다(18:30 · 19:00 · 19:30 세 번) | ok 수가 18 아래로 안 떨어짐 | 대장 M8 에 실측 기록. 코드는 안 고친다(동결) |
| 19:00~21:00 | 전야제 인파 — 여의나루·여의도 `live_snapshot.congest` 가 「여유」에서 움직이는지, α 가 사전값 1.00 에서 갱신되는지(`α=`, `prior=False`) | 21시 전 `prior=False` | `prior=True` 가 22시까지 가면 하차 관측이 안 들어온 것 — §3-③ |
| 20:00 | **API 시차 30분 가설** — `api jsonl` 의 `ppltn_time` 과 `ts` 차이를 세 레코드 적어 둔다 | 차이 일정(≈30분) | 들쭉날쭉이면 대장에 기록 |
| 22:00 | 전야제 종료. **끄지 않는다.** `status.sh` 저장: `./tools/status.sh > logs/status_0904_2200.txt` | 무중단 2h 이상(스펙 §4 ✅) | 재기동·공백 있으면 §4 사후 점검 |
| 22:30 | 합격선 판정 — §2 | | |
| 23:55 | 마지막 점검 후 취침. **뚜껑 덮지 않는다** | | |

### 9/5(토) 본번 — 쇼 20:00~21:10(영국 20:00 · 미국 20:20 · 한국 ~20:40)

| 시각 | 확인 | 정상이면 | 아니면 |
| :-- | :-- | :-- | :-- |
| 00:05 | (자기 전이면) 날짜 넘김 — `data/live/api_20260905.jsonl`·`cctv_20260905.jsonl` 생김 | 두 파일 존재 | 없으면 수집기 죽음 → §3-① |
| 09:00 | `status.sh` — **시간 공백** 줄. 밤새 공백이 있으면 그 구간이 슬립이다 | 「없음」 | §4 로 기록, 원인(전원·뚜껑) 제거 |
| 10:00 | **최종 동결.** 이후 `src/`·`docs/index.html`·`docs/go.html` 은 hotfix 만, `pytest` 통과 후 push. **커밋은 발행 틱(매 :x8·:x3 분 ±) 직후에** — publish 가 push 중이면 충돌 | | |
| 12:00 | 피더 12곳 수집 시작(12~19시) — `api.log` 에 피더 역 이름 | | 피더키 오류면 일반키로 넘어간다(설계) |
| 17:00 | 지하철 실시간 도착 시작. `status.sh` | | |
| 18:00 | 관람객·운영 화면 폰으로 한 번 — 지도·혼잡장 색·경로선·필 「실시간」 | | 지도만 안 뜨면 화면 문제(수집은 무관) |
| 18:50 | 일몰 — 전야제와 같은 M8 기록 | | |
| 20:00 | 쇼 시작. 5분마다 `status.sh` 안 봐도 된다 — 워치독이 본다. 30분에 한 번 | | |
| **20:55** | **쇼 종료 기입 담당 알람.** 마지막 불꽃 직후: | | |
|  | `echo 21:10 > data/live/show_end.txt` (실제 시각으로) | 다음 틱 `latest.json` `show_end_source: "file"`, `show_end_actual` 그 시각 | `planned_invalid_file` 이면 형식 오류(HH:MM, 19:00~24:00) — 다시 쓴다 |
| 20:40~21:40 | 여의나루 무정차 — 두 화면에서 「통제」 표시 | | |
| 21:10~23:00 | **귀가 피크.** 30분마다 `status.sh`. 발행 실패·수집 끊김만 본다 | | §3 |
| 23:30 | 마지막 점검, `./tools/status.sh > logs/status_0905_2330.txt` | | |
| **00:00** | `kill -INT 54527` → `status.sh` 프로세스 전부 0 → `git status` 깨끗(발행 커밋 다 올라갔나) | | 남은 프로세스는 `pkill -f collector_` |
| 9/6 오전 | `.venv/bin/python src/evaluate.py --date 20260905` → 오차표. 전야제는 `--fallback rehearsal` | | |

## 2. 전야제 합격선 — 의미와 미달 시 행동 (2026-09-04 결정 제안, 미확정)

합격선 = 여의나루 하차가 **평시 금요일의 1.9배**(`data/derived/eve_rehearsal.json`, OA-12921 2년 실적). 당일엔 공식 하차 실측이 없으므로 **22:30 에 `evaluate.py --fallback rehearsal` 이 내는 30분 승차·도착 형태로 대신 본다.**

- **달성** → 야간 CCTV·API 시차·α 갱신이 실제 인파에서 확인된 것. 9/5 는 그대로 간다.
- **미달** → 전야제가 리허설로 못 쓰인 것이지 모델 결함이 아니다. **α 사전분포·사전표를 바꾸지 않는다.** 보고서엔 "전야제 인파가 합격선에 못 미쳐 야간 보정은 9/5 당일 데이터로 대체"라고 적는다.
- 어느 쪽이든 코드 변경 없음 — 10:00 최종 동결 전에 손댈 것은 전야제에서 **깨진 것**(트레이스백·재기동 루프)뿐이다.

- [ ] 위 규칙 확정 (사용자)

## 3. 무엇이 깨지면

| 증상 (`status.sh`) | 원인 후보 | 조치 |
| :-- | :-- | :-- |
| ① 프로세스 0 · 시간 공백 | 맥 슬립(뚜껑·배터리) · 재시동 | 전원·뚜껑 바로잡고 `nohup ./run_all.sh > logs/run_$(date +%m%d%H%M).log 2>&1 &`. 새 pid 를 이 문서 상단에 적는다. 공백은 §4 에 기록 |
| ② `api_last_ok` 10분 초과 · 필 「수집 끊김」 · `degraded=['citydata']` | 키 오류·쿼터·네트워크 | `tail -3 logs/api.log`. 키 문제면 `.env` 고친 뒤 **`kill $(pgrep -f collector_api.py)`** — 수집기는 import 때 키를 읽어 kill 해야 새 키로 뜬다(워치독이 30초 안에 재기동). 네트워크면 Wi‑Fi |
| ③ `prior=True` 가 저녁까지 | 하차 관측(subway) 안 들어옴 | `api.log` 에 `trains`/하차 줄 있나. 없으면 지하철 키 6개 확인. 있으면 정상 창(O1 12~18시·O2 19시~) 밖일 수 있다 |
| ④ 발행 실패 누적 | push 거절·네트워크 | `git status` · `git log origin/main..HEAD`. publish 는 pull --rebase 후 1회 재시도한다. 손으로: `git pull --rebase --autostash && git push` |
| ⑤ 워치독 「크래시 루프」 | 수집기 시작 즉시 죽음 | `tail logs/api.log logs/cctv.log` 트레이스백. 동결 중이라 코드 수정은 hotfix 절차(pytest → push) |
| ⑥ CCTV ok 급감 | 스트림 끊김·야간(M8) | 5대 미만이면 `CAMS=192,725,310,331,39` 로 ROI 5대만 재기동 고려. 아니면 기록만 |
| ⑦ Pages 가 옛 시각 | GitHub 배포 지연(1~2분) · 발행 실패 | 3분 기다려 새로고침. 그래도면 ④ |
| ⑧ 디스크 | — | 120GB 여유. 하루 CCTV jsonl ≈ 4MB. 문제 없음 |

## 4. 사후 점검 (9/5 09:00 · 9/6)

- `status.sh` 「시간 공백」 — 공백 = 슬립. 있으면 대장 C6 에 시각과 원인을 적는다
- `logs/watchdog.log` 의 `WATCHDOG:` 줄 수와 사유
- 전야제 M8 기록(18:30·19:00·19:30 CCTV ok 수·bg_fail 대수) → 대장 M8
- API 시차(20:00 기록) → 대장

## 5. 하지 말 것

- 뚜껑 덮기 · 배터리 운전 · 시스템 재시동 · macOS 업데이트
- 발행 틱 중 `git push`(publish 와 경합) — `status.sh` 「마지막 발행」 직후 1분 안에 한다
- 동결 후 `src/`·화면 변경을 테스트 없이 push
- `.env` 값을 어디에도 적지 않는다 — 이 문서 포함
