# 피치 덱 v2 구현 플랜 — 5분 · 12장 · 라이트 테마 · 3층 해부

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/deck.html` 을 12장 라이트 테마 덱으로 제자리 교체한다 — 관람객·운영 화면은 iframe 실물, 피더 12곳 방사형 도식·α 격자·혼잡장·경로·백테스트는 `docs/deck/*.json` 에서 그리고, 코드 스트립은 소스에서 빌드 시 잘라 넣는다.

**Architecture:** 한 파일 `docs/deck.html`(CSS 토큰 + 12 `<section>` + 하나의 IIFE 스크립트)이 `docs/deck/*.json` 을 fetch 해 canvas/SVG/DOM 으로 그린다. 데이터는 `tools/deck_data.py`(파이썬, `data/derived` 와 소스 파일 → JSON)와 기존 `tools/deck_field_route.mjs` 가 만든다. `tests/test_deck.py` 가 HTML 의 손글씨 숫자·구조·임베드 규칙과 JSON ↔ 원본 정합을 정적으로 검사한다.

**Tech Stack:** 순수 HTML/CSS/JS(라이브러리 없음, KaTeX 0.16.11 만 CDN) · Python 3 + pytest(`.venv`) · Pretendard(jsDelivr 동적 서브셋) · IBM Plex Mono · Black Han Sans(Google Fonts) · 헤드리스 Chrome(검증만)

**Spec:** `docs/superpowers/specs/2026-09-04-deck-v2-design.md` — 플랜은 이 스펙을 근거로 쓴다. 실행자는 둘 다 읽는다.

## Global Constraints

- **건드리지 않는 파일:** `src/**` · `docs/index.html` · `docs/go.html` · `docs/app/**` (코드 동결 이후. 임베드는 파라미터 없이 있는 그대로 띄운다)
- **덱 데이터는 `docs/deck/`** — `docs/data/` 아래 두지 않는다(`src/publish.py:109` 가 `git add docs/data` 로 통째 스테이징)
- **git:** 수집기가 5분마다 `docs/data` 를 커밋·push 하는 중이다. `git add` 는 **파일 명시**만, `git add -A` 금지. 작업 트리에 `data/live/forecast_latest.json` 수정본이 늘 있으니 `git pull --rebase` 는 `--autostash` 를 붙인다. 커밋은 태스크마다 — 수집기의 다음 push 가 그 커밋을 함께 올린다
- **테마 토큰(값 그대로):** `--bg:#F5F4F1` · `--sheet:#FFFFFF` · `--ink:#14110C` · `--ink-2:#5B554B` · `--sub:#8F897D` · `--rule:#E2DFD8` · `--accent:#F36F21`(도형·바·진행선에만, 글자엔 안 쓴다) · `--live:#2A78D6` · `--offline:#D0342C` · 등급 `--g1:#2E7D5B --g2:#8A6400 --g3:#B0521C --g4:#B3352B`(status 전용) · 시리즈 `--c1:#e2661c --c2:#4f95e8`(2026-09-04 dataviz 검증기 `--mode light` 표면 `#FFFFFF` 전부 PASS: CVD ΔE 26.9 · 대비 ≥3:1)
- **금지 문자열(테스트가 본다):** `#0b0d12` · `Hahmlet` · `fireworks-js` · `allow="geolocation"`
- **한국어 산출물은 번역투 금지** — 피동·만연체·"~에 대한" 을 쓰지 않는다. 카피는 이 플랜의 문장을 그대로 쓴다
- **검증 명령:** `.venv/bin/python -m pytest tests -q` (시작 시점 96건 통과) · 데이터 재생성 `.venv/bin/python tools/deck_data.py`
- **옛 덱 참조:** `git show fba7bac:docs/deck.html` — 이식할 함수는 이 커밋의 줄 번호로 가리킨다

---

## 파일 구조

| 파일 | 역할 | 이 플랜에서 |
|:--|:--|:--|
| `tools/deck_data.py` | `data/derived/*` · 소스 파일 · 결함 대장 → `docs/deck/*.json` | 5개 함수 추가(exit_bars · feeder_map · redteam_counts · code_strips · live_result) |
| `docs/deck/*.json` | 덱이 fetch 하는 데이터 | 신규 5개, 기존 5개 유지 |
| `docs/deck.html` | 덱 본체 (CSS · 12장 마크업 · 스크립트) | 전면 교체 |
| `tests/test_deck.py` | 정적 검사 | 키 집합 갱신 + 신규 9건 |
| `README.md` | 덱 행 2곳 + 「발표」 절 | 갱신 |
| `docs/deck/fallback_go.png` · `fallback_ops.png` | 오프라인 폴백 캡처 | **사용자가 찍는다** — 이 플랜은 파일 유무와 무관하게 동작하게만 만든다 |

---

### Task 1: 데이터 내보내기 5종 + 테스트

**Files:**
- Modify: `tools/deck_data.py` (전체 134줄 — `main()` 앞에 함수 5개 추가, `main()` 목록 확장)
- Test: `tests/test_deck.py` (끝에 추가)

**Interfaces:**
- Produces (JSON, 덱 스크립트가 읽는다):
  - `docs/deck/exit_bars.json` — `{"exits":[{"name":str,"share":float,"E":int}...7, 비중 내림차순], "total":int, "festival_days":[str], "source":str}`
  - `docs/deck/feeder_map.json` — `{"center":{"name":"여의도","lat":float,"lng":float}, "rings_min":[10,20,30], "feeders":[{"name","lines":[str],"gu","persons":int,"share":float,"travel_min":int,"bearing_deg":float,"r_lag0":float,"r_lag1":float,"phase":str}...12, persons 내림차순], "travel_min_basis":str, "source":str}`
  - `docs/deck/redteam_counts.json` — `{"by_grade":{"치명":int,"높음":int,"중간":int,"낮음":int}, "ids":{같은 키:[str]}, "total":int, "retracted":[str], "source":"redteam-20260901.md", "rule":str}`
  - `docs/deck/code_strips.json` — `[{"id":"demand"|"alpha"|"blend","file":str,"start":int,"lines":[str],"caption":str}]`
  - `docs/deck/live_result.json` — 초기 `{"filled":false,"date":"2026-09-05","note":str}`. 채워지면 `{"filled":true,"date","grade_hit":str,"alpha_final":str,"ticks":int,"restarts":int,"note":str}` (9/6 에 만든다 — 이 플랜 범위 밖)

- [ ] **Step 1: 실패하는 테스트 5건을 `tests/test_deck.py` 끝에 추가**

```python


# ── v2 (2026-09-04) — 내보내기 5종 ────────────────────────────────────
def test_exit_bars_are_seven_exits_summing_to_one():
    d = _json("exit_bars")
    assert len(d["exits"]) == 7
    assert abs(sum(e["share"] for e in d["exits"]) - 1.0) < 0.01
    shares = [e["share"] for e in d["exits"]]
    assert shares == sorted(shares, reverse=True), "비중 내림차순이어야 막대 순서가 읽힌다"
    src = json.loads((DER / "exit_shares.json").read_text(encoding="utf-8"))
    for e in d["exits"]:
        assert e["share"] == src["share_mean"][e["name"]]
        assert e["E"] == src["E_mean"][e["name"]]


def test_feeder_map_has_twelve_stations_with_bearing():
    d = _json("feeder_map")
    src = json.loads((DER / "feeder_leadlag.json").read_text(encoding="utf-8"))
    assert len(d["feeders"]) == 12
    assert {f["name"] for f in d["feeders"]} == set(src["top_feeders"])
    persons = [f["persons"] for f in d["feeders"]]
    assert persons == sorted(persons, reverse=True)
    for f in d["feeders"]:
        assert 0 <= f["bearing_deg"] < 360, f
        assert f["travel_min"] > 0 and f["persons"] > 0, f
        assert f["persons"] == src["top_feeders"][f["name"]]["attributed_2yr"]
    assert d["rings_min"] == [10, 20, 30]
    assert d["center"]["name"] == "여의도"


def _ledger_ids():
    """결함 대장에서 ID 를 세는 규칙 — 표 첫 칸(`| C2 …` · `| **M4** …`) 또는 `### M4.` 제목. `### 철회 — H8` 은 철회."""
    txt = (ROOT / "redteam-20260901.md").read_text(encoding="utf-8")
    listed = set(re.findall(r"^\| *\**([CHML]\d+)\b", txt, re.M)) | set(re.findall(r"^### ([CHML]\d+)\.", txt, re.M))
    retracted = set(re.findall(r"^### 철회 — ([CHML]\d+)", txt, re.M))
    return listed - retracted, retracted


def test_redteam_counts_match_ledger():
    d = _json("redteam_counts")
    listed, retracted = _ledger_ids()
    assert d["total"] == len(listed)
    assert sum(d["by_grade"].values()) == d["total"]
    assert set(d["retracted"]) == retracted
    for g, key in (("C", "치명"), ("H", "높음"), ("M", "중간"), ("L", "낮음")):
        assert d["by_grade"][key] == len([i for i in listed if i[0] == g]), key


def test_code_strips_match_source():
    """덱의 코드 스트립은 손으로 베낀 게 아니라 소스에서 잘라 온 것이어야 한다."""
    strips = _json("code_strips")
    assert [s["id"] for s in strips] == ["demand", "alpha", "blend"]
    for s in strips:
        lines = (ROOT / s["file"]).read_text(encoding="utf-8").splitlines()
        seg = lines[s["start"] - 1: s["start"] - 1 + len(s["lines"])]
        assert seg == s["lines"], f"{s['id']}: {s['file']}:{s['start']} 가 소스와 다르다 — tools/deck_data.py 재실행"
        assert 3 <= len(s["lines"]) <= 8


def test_live_result_placeholder_exists():
    d = _json("live_result")
    assert isinstance(d["filled"], bool)
    if d["filled"]:
        for k in ("grade_hit", "alpha_final", "ticks", "restarts"):
            assert k in d, k
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/bin/python -m pytest tests/test_deck.py -q -k "exit_bars or feeder_map or redteam_counts or code_strips or live_result"`
Expected: 5 skipped (`docs/deck/*.json 없음 — tools/deck_data.py 먼저 실행`) — `_json` 이 파일 없으면 skip 한다. 파일을 만들면 assert 로 넘어간다.

- [ ] **Step 3: `tools/deck_data.py` 에 함수 5개 추가 — `def main():` 바로 앞에 삽입**

파일 맨 위 import 줄 `import json, math, pathlib, sys` 를 `import json, math, pathlib, re, sys` 로 바꾼다. 그리고 `def main():` 앞에:

```python
# ── ④ 출구 7개 실측 비중 (사전층 ①) ──────────────────────────────────
def exit_bars():
    """E_st — 작년에 실제로 그 출구로 나간 인원의 비중. 2년 평균, 초과 승차 기준(exit_shares.py)."""
    d = _load("exit_shares")
    rows = [{"name": k, "share": v, "E": d["E_mean"][k]}
            for k, v in sorted(d["share_mean"].items(), key=lambda kv: -kv[1])]
    return {"exits": rows, "total": d["total_mean"], "festival_days": d["festival_days"],
            "source": "data/derived/exit_shares.json share_mean·E_mean (2년 평균 초과 승차)"}


# ── ⑤ 피더 12곳 방사형 (사전층 ②) ────────────────────────────────────
def _bearing(lat1, lng1, lat2, lng2):
    """(lat1,lng1) 에서 본 (lat2,lng2) 의 방위각 — 북 0°, 시계 방향."""
    dl = math.radians(lng2 - lng1)
    p1, p2 = math.radians(lat1), math.radians(lat2)
    x = math.sin(dl) * math.cos(p2)
    y = math.cos(p1) * math.sin(p2) - math.sin(p1) * math.cos(p2) * math.cos(dl)
    return (math.degrees(math.atan2(x, y)) + 360) % 360


def _coord(stations, name):
    """역 좌표. all_coords 가 '낙성대'·'잠실' 처럼 짧은 이름을 갖고, stations 는 '낙성대(강감찬)' 식이라 all_coords 먼저."""
    ac = stations["all_coords"].get(name)
    if ac:
        return ac[0]["lat"], ac[0]["lng"]
    st = stations["stations"].get(name)
    if st:
        return st["lat"], st["lng"]
    raise KeyError(f"stations.json 에 {name} 좌표가 없다")


def feeder_map():
    """보고서 그림 1 과 같은 도식 — 방위는 실제, 중심 거리는 지하철 소요시간(추정), 원 크기는 2년 귀속 인원."""
    d = _load("feeder_leadlag")
    st = _load("stations")
    clat, clng = _coord(st, "여의도")
    rows = []
    for name, v in d["top_feeders"].items():
        lat, lng = _coord(st, name)
        rows.append({"name": name, "lines": v["lines"], "gu": v["gu"],
                     "persons": v["attributed_2yr"], "share": v["share_of_attributed"],
                     "travel_min": v["travel_min_est"], "bearing_deg": round(_bearing(clat, clng, lat, lng), 1),
                     "r_lag0": v["pearson_lag0_pooled"], "r_lag1": v["pearson_lag1_pooled"], "phase": v["phase"]})
    rows.sort(key=lambda r: -r["persons"])
    return {"center": {"name": "여의도", "lat": clat, "lng": clng}, "rings_min": [10, 20, 30],
            "feeders": rows, "travel_min_basis": d.get("travel_min_basis", ""),
            "source": "data/derived/feeder_leadlag.json top_feeders · stations.json 좌표"}


# ── ⑥ 결함 대장 건수 (검증) ──────────────────────────────────────────
GRADE_KO = {"C": "치명", "H": "높음", "M": "중간", "L": "낮음"}


def redteam_counts():
    """redteam-20260901.md 에 등재된 결함 ID 를 센다 — 표 첫 칸 또는 `### ID.` 제목. `### 철회 — ID` 는 따로."""
    txt = (ROOT / "redteam-20260901.md").read_text(encoding="utf-8")
    listed = set(re.findall(r"^\| *\**([CHML]\d+)\b", txt, re.M)) | set(re.findall(r"^### ([CHML]\d+)\.", txt, re.M))
    retracted = set(re.findall(r"^### 철회 — ([CHML]\d+)", txt, re.M))
    listed -= retracted
    by = {g: sorted((i for i in listed if i[0] == g), key=lambda s: int(s[1:])) for g in "CHML"}
    return {"by_grade": {GRADE_KO[g]: len(by[g]) for g in "CHML"},
            "ids": {GRADE_KO[g]: by[g] for g in "CHML"},
            "total": len(listed), "retracted": sorted(retracted),
            "source": "redteam-20260901.md",
            "rule": "표 첫 칸 `| ID` 또는 `### ID.` 제목의 고유 ID. `### 철회 — ID` 는 철회로 분리"}


# ── ⑦ 코드 스트립 — 소스에서 앵커를 찾아 N줄 ─────────────────────────
STRIPS = [
    ("demand", "src/backtrack.py", "def exit_forecast(", 6, "사전층 — 유출 곡선을 쇼 종료만큼 밀고 출구에 배분한다"),
    ("alpha", "src/nowcast.py", "def assimilate(", 8, "당일층 — 격자 사후분포 w(α) ∝ 사전 × Π N(y; A·α+B, σ)"),
    ("blend", "docs/app/field.js", "function blendSeconds(", 3, "화면층 — 간선을 걷는 시간 = 거리 ÷ 속도(확신도 혼합 밀도)"),
]


def code_strips():
    """손으로 베끼지 않는다. 앵커(함수 시그니처)를 찾아 그 줄부터 N줄. 줄 번호는 지금 계산한다."""
    out = []
    for sid, rel, anchor, n, cap in STRIPS:
        lines = (ROOT / rel).read_text(encoding="utf-8").splitlines()
        idx = next(i for i, l in enumerate(lines) if anchor in l)
        out.append({"id": sid, "file": rel, "start": idx + 1, "lines": lines[idx: idx + n], "caption": cap})
    return out


# ── ⑧ 9/5 실전 결과 자리 ─────────────────────────────────────────────
def live_result():
    """채워진 파일은 덮어쓰지 않는다. 비어 있으면 자리만.

    채울 때 형태(9/6, evaluate.py 결과로): {"filled": true, "date": "2026-09-05",
    "grade_hit": "88% (15/17)", "alpha_final": "1.08 [0.95~1.21]", "ticks": 144, "restarts": 0, "note": "…"}
    """
    p = OUT / "live_result.json"
    if p.exists():
        cur = json.loads(p.read_text(encoding="utf-8"))
        if cur.get("filled"):
            return cur
    return {"filled": False, "date": "2026-09-05",
            "note": "9/6 evaluate.py 결과로 채운다. 12~24시 자동 수집 · 5분 발행 · 쇼 종료 실시각 현장 기입"}


```

그리고 `main()` 의 튜플 목록을 이렇게 바꾼다:

```python
    for name, fn in (("feeder_lag", feeder), ("backtest_bars", backtest), ("alpha_grid", alpha),
                     ("exit_bars", exit_bars), ("feeder_map", feeder_map), ("redteam_counts", redteam_counts),
                     ("code_strips", code_strips), ("live_result", live_result)):
```

- [ ] **Step 4: 내보내기 실행 → 파일 8개 확인**

Run: `.venv/bin/python tools/deck_data.py && ls docs/deck/`
Expected: 8개 `.json` 줄이 크기와 함께 찍히고 `ls` 에 `exit_bars.json feeder_map.json redteam_counts.json code_strips.json live_result.json` 이 보인다. `feeder_map` 은 12개 역 전부 좌표를 찾아야 한다 — `KeyError` 가 나면 `stations.json` 의 `all_coords` 키를 확인한다(2026-09-04 확인: 낙성대·잠실 포함 12곳 모두 `all_coords` 에 있다).

- [ ] **Step 5: 통과 확인**

Run: `.venv/bin/python -m pytest tests/test_deck.py -q`
Expected: 기존 9건 + 신규 5건 = 14 passed. (`redteam_counts` 는 2026-09-04 기준 등재 36 · 철회 1(H8) 이어야 한다 — 다르면 정규식이 아니라 대장이 바뀐 것이니 값을 보고 판단)

- [ ] **Step 6: 커밋**

```bash
git add tools/deck_data.py tests/test_deck.py docs/deck/exit_bars.json docs/deck/feeder_map.json docs/deck/redteam_counts.json docs/deck/code_strips.json docs/deck/live_result.json
git commit -m "deck: 내보내기 5종 — 출구 비중·피더 방사형·결함 건수·코드 스트립·실전 결과 자리"
```

---

### Task 2: `docs/deck.html` 전면 교체 — 테마 · 12장 마크업 · 크롬 · 임베드 · 스트립 (차트 함수는 비워 둔다)

이 태스크가 끝나면 덱은 **차트 없이도 완결된 문서**다. 모든 카피가 들어가므로 옛 덱의 숫자 정합 테스트(경로·피더 r·백테스트 %·가정 라벨)가 그대로 통과한다. 차트 함수는 Task 3~5 가 `/* charts:functions */` 마커 자리에 넣는다.

**Files:**
- Rewrite: `docs/deck.html`
- Modify: `tests/test_deck.py` — `test_every_chart_has_its_data_file` 교체 + 구조 테스트 4건 추가

**Interfaces:**
- Produces (스크립트 안, 뒤 태스크가 쓴다): `CHARTS` 객체(`CHARTS.<key> = ["<json 이름>", fn]`, `fn(cv, d, slide, fig)` — `cv` 는 figure 안 첫 canvas 또는 null) · `animate(cv, ms, draw)` · `fitCanvas(cv)` · `axes(c,w,h,pad)` · `label(c,txt,x,y,col,align,size,font)` · `cssVar(k)` · `hex(s)` · `countUp(slide)` · `CHART_REDUCE` · `chartData(name)`
- 마커: 스크립트의 `/* charts:functions */` 한 줄 — 뒤 태스크가 이 줄 **앞**에 함수를 넣는다

- [ ] **Step 1: 테스트 교체·추가 — `tests/test_deck.py`**

`test_every_chart_has_its_data_file` 함수 전체(39~43줄)를 이걸로 바꾼다:

```python
CHART_FILES = {"exits": "exit_bars", "radial": "feeder_map", "feeder": "feeder_lag", "alpha": "alpha_grid",
               "field": "field_grid", "route": "route_demo", "backtest": "backtest_bars",
               "redteam": "redteam_counts", "live": "live_result"}


def test_every_chart_has_its_data_file(html):
    keys = set(re.findall(r'data-chart="([a-z]+)"', html))
    assert keys <= set(CHART_FILES), keys - set(CHART_FILES)
    for k in keys:
        assert (DATA / f"{CHART_FILES[k]}.json").exists(), f"{CHART_FILES[k]}.json 이 없다"
```

파일 끝에 추가:

```python


# ── v2 — 구조 ─────────────────────────────────────────────────────────
def _sections(html):
    return re.findall(r'<section class="slide[^"]*" id="(s\d+)">(.*?)</section>', html, re.S)


def test_twelve_sections_each_with_heading_and_notes(html):
    secs = _sections(html)
    assert [s[0] for s in secs] == [f"s{i}" for i in range(1, 13)]
    for sid, body in secs:
        assert re.search(r"<h[12]\b", body), f"{sid}: 제목 없음"
        assert '<aside class="notes">' in body, f"{sid}: 발표자 노트 없음"


def test_light_theme_tokens_and_no_dark_leftovers(html):
    for tok in ("--bg:#F5F4F1", "--sheet:#FFFFFF", "--ink:#14110C", "--rule:#E2DFD8", "--accent:#F36F21"):
        assert tok in html, tok
    for bad in ("#0b0d12", "Hahmlet", "fireworks-js"):
        assert bad not in html, f"검정 덱 잔재: {bad}"


def test_embeds_are_real_screens_without_geolocation_prompt(html):
    go = re.search(r"<iframe[^>]*src=\"go\.html\"[^>]*>", html)
    assert go, "go.html iframe 이 없다"
    assert "allow=" not in go.group(0), "위치 권한을 주면 발표 중 팝업이 뜬다 — allow 속성 금지"
    assert re.search(r"<iframe[^>]*src=\"index\.html\"", html), "index.html iframe 이 없다"
    assert "deck/fallback_go.png" in html and "deck/fallback_ops.png" in html


def test_code_strip_slots_match_export(html):
    slots = re.findall(r'<pre class="strip[^"]*" data-strip="([a-z]+)"', html)
    assert slots == ["demand", "alpha", "blend"]
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/bin/python -m pytest tests/test_deck.py -q`
Expected: 구조 4건 FAIL (옛 덱은 18 섹션·검정 토큰), 나머지 통과.

- [ ] **Step 3: `docs/deck.html` 을 아래 내용으로 통째로 쓴다**

```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>불꽃축제 귀가 내비 — 피치 덱</title>
<link rel="icon" href="assets/favicon.ico" sizes="any">
<link rel="icon" type="image/png" href="assets/favicon.png">
<link rel="apple-touch-icon" href="assets/apple-touch-icon.png">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600&family=Black+Han+Sans&display=swap">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css">
<style>
:root{
  --bg:#F5F4F1; --sheet:#FFFFFF; --ink:#14110C; --ink-2:#5B554B; --sub:#8F897D; --rule:#E2DFD8;
  --accent:#F36F21; --live:#2A78D6; --offline:#D0342C;
  --g1:#2E7D5B; --g2:#8A6400; --g3:#B0521C; --g4:#B3352B;   /* 등급 — go.html 과 같은 값, status 전용 */
  --c1:#e2661c; --c2:#4f95e8;   /* 시리즈 2색 — 2026-09-04 dataviz 검증기 light/#FFFFFF PASS (CVD ΔE 26.9) */
  --line:var(--rule); --panel:var(--sheet);   /* 옛 차트 코드가 쓰는 이름 */
  --sans:"Pretendard Variable",Pretendard,-apple-system,BlinkMacSystemFont,"Apple SD Gothic Neo",system-ui,sans-serif;
  --mono:"IBM Plex Mono",ui-monospace,monospace;
  color-scheme:light only;
}
*{box-sizing:border-box}
html,body{height:100%}
body{margin:0;background:var(--bg);color:var(--ink);font:clamp(15px,1.55vw,20px)/1.6 var(--sans);overflow:hidden;-webkit-font-smoothing:antialiased}
a{color:var(--ink);text-decoration:underline;text-decoration-color:var(--rule)}

/* ── 슬라이드 골격 ── */
.slide{position:absolute;inset:0;padding:5vh 6vw 8vh;display:flex;flex-direction:column;justify-content:center;
  opacity:0;visibility:hidden;transform:translateY(14px);transition:opacity .4s ease,transform .4s ease,visibility 0s .4s}
.slide.active{opacity:1;visibility:visible;transform:none;transition:opacity .4s ease,transform .4s ease}
.kicker{font:600 clamp(11px,.95vw,13px) var(--mono);letter-spacing:.14em;color:var(--sub);margin-bottom:1.6vh;text-transform:uppercase}
.kicker::before{content:"";display:inline-block;width:18px;height:3px;background:var(--accent);margin-right:10px;vertical-align:middle;border-radius:2px}
h1{font-weight:800;font-size:clamp(34px,5.2vw,68px);line-height:1.2;letter-spacing:-.02em;margin:0 0 2.4vh}
h2{font-weight:800;font-size:clamp(26px,3.3vw,44px);line-height:1.28;letter-spacing:-.015em;margin:0 0 2.6vh}
h2 em,h1 em{font-style:normal;text-decoration:underline;text-decoration-color:var(--accent);text-decoration-thickness:.12em;text-underline-offset:.12em}
#s1 h1{font-family:"Black Han Sans",var(--sans);font-weight:400;letter-spacing:.005em;font-size:clamp(40px,6vw,82px);line-height:1.16}
.sub{color:var(--ink-2)}
.big{font:600 clamp(56px,10vw,132px)/1 var(--mono);letter-spacing:-.04em;color:var(--ink);margin:.4vh 0 1vh}
.big small{font-size:.32em;color:var(--sub);font-weight:400;letter-spacing:0}
.cols{display:flex;gap:2.6vw;flex-wrap:wrap;align-items:stretch}
.cols>*{flex:1;min-width:240px}
.cols.w2>:first-child{flex:1.15}
ul.pts{list-style:none;margin:0;padding:0;max-width:60ch}
ul.pts li{padding:.9vh 0 .9vh 24px;position:relative}
ul.pts li::before{content:"";position:absolute;left:1px;top:calc(.9vh + .6em);width:9px;height:9px;background:var(--accent);border-radius:2px;transform:rotate(45deg)}
ul.pts b{color:var(--ink)}
ol.steps{margin:0;padding-left:1.4em}
ol.steps li{padding:.5vh 0}
ol.steps b{color:var(--ink)}
.card{background:var(--sheet);border:1px solid var(--rule);border-radius:14px;padding:2.2vh 1.8vw;
  box-shadow:0 1px 2px rgba(20,17,12,.04),0 10px 28px rgba(20,17,12,.06)}
.card h3{margin:0 0 .8vh;font-size:clamp(15px,1.4vw,19px);font-weight:800}
.card p{margin:.4vh 0 0;font-size:clamp(13px,1.15vw,16px);color:var(--ink-2);line-height:1.5}
.card .q{font-size:clamp(20px,2.2vw,30px);font-weight:800;letter-spacing:-.01em;margin:.2vh 0 .8vh}
.formula{font-weight:800;font-size:clamp(22px,2.8vw,38px);text-align:center;padding:2.2vh 1vw}
.src{font-size:clamp(11px,.98vw,13.5px);color:var(--sub);margin-top:1.8vh;line-height:1.5}
.mono{font-family:var(--mono)}
.grade{display:inline-block;padding:.05em .6em;font-size:.78em;font-weight:700;color:#fff;border-radius:4px;white-space:nowrap}
.g1{background:var(--g1)}.g2{background:var(--g2)}.g3{background:var(--g3)}.g4{background:var(--g4)}
.tag{display:inline-block;font:600 clamp(9.5px,.88vw,11.5px) var(--mono);letter-spacing:.04em;padding:.2em .6em;border:1px solid var(--rule);color:var(--ink-2);border-radius:4px;background:var(--sheet)}
.tag.warn{border-color:#E7C9A6;background:#FFF4E8;color:#8A4A12}
.pill{display:inline-block;font:700 12px var(--sans);padding:.25em .7em;border-radius:999px;color:#fff;background:var(--live)}
.pill.off{background:var(--offline)}
table{border-collapse:collapse;width:100%;font-size:clamp(12.5px,1.15vw,16px)}
th,td{border-bottom:1px solid var(--rule);padding:.9vh .8vw;text-align:left}
th{border-top:2px solid var(--ink);font-weight:750}
td.num,th.num{text-align:right;font-family:var(--mono)}

/* ── 차트 ── */
.fig{margin:.8vh 0 0;position:relative}
.fig canvas{display:block;width:100%;height:min(40vh,380px)}
.fig.mini canvas{height:min(22vh,200px)}
.fig.duo{display:grid;grid-template-columns:1fr 1fr;gap:1vw}
.fig.duo canvas{height:min(40vh,380px);background:var(--sheet);border:1px solid var(--rule);border-radius:10px}
.fig .radial{width:100%;max-width:min(58vh,560px);margin:0 auto}
.fig .radial svg{width:100%;height:auto;display:block}
.lg{display:flex;gap:1.4vw;flex-wrap:wrap;align-items:center;font:600 clamp(10.5px,.95vw,13px) var(--mono);color:var(--ink-2);margin-top:.8vh}
.lg i{display:inline-block;width:11px;height:11px;margin-right:6px;vertical-align:-1px;border-radius:3px}
.lg b{color:var(--ink);font-weight:600}
.katex{font-size:1.02em}
.katex-display{margin:1vh 0;overflow-x:auto;overflow-y:hidden}

/* 코드 스트립 — 진짜 도는 코드라는 신호. 25초엔 못 읽으니 작게 */
pre.strip{margin:1.2vh 0 0;padding:1.3vh 1.2vw;background:#1C1916;color:#EDE7DD;border-radius:10px;font:500 clamp(11px,.95vw,13.5px)/1.5 var(--mono);
  overflow:hidden;white-space:pre;tab-size:4}
pre.strip .ln{display:block;color:#A79F92;font-weight:600;margin-bottom:.5em}

/* 실물 임베드 — 폰 프레임 · 운영 화면. 화면 크기에 맞춰 JS 가 scale 을 잡는다 */
.embed{margin:0;position:relative;display:flex;align-items:center;justify-content:center;height:min(74vh,780px)}
.embed .frame{position:relative;overflow:hidden;background:var(--sheet);border:1px solid var(--rule);
  box-shadow:0 2px 6px rgba(20,17,12,.06),0 24px 60px rgba(20,17,12,.16)}
.embed.phone .frame{border-radius:34px;border:6px solid #1C1916}
.embed.ops .frame{border-radius:12px}
.embed .screen{position:absolute;left:0;top:0;transform-origin:top left}
.embed.phone .screen{width:390px;height:844px}
.embed.ops .screen{width:1280px;height:800px}
.embed iframe{border:0;width:100%;height:100%;display:block;background:var(--sheet)}
.embed img.fb{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}
.embed .fbmsg{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;color:var(--sub);font:600 14px var(--mono)}

/* 3층 스택 (4장) */
.stack{display:flex;flex-direction:column;gap:1.2vh}
.stack .lay{border:1px solid var(--rule);background:var(--sheet);border-radius:12px;padding:1.5vh 1.4vw;display:flex;gap:1.4vw;align-items:baseline}
.stack .lay b{white-space:nowrap;color:var(--ink);font-size:1.05em}
.stack .lay span{color:var(--ink-2);font-size:.92em}
.stack .arr{text-align:center;color:var(--sub);font-size:.88em;margin:-.4vh 0}

/* 숫자 타일 (9·10장) */
.tiles{display:grid;grid-template-columns:repeat(4,1fr);gap:1vw}
.tile{background:var(--sheet);border:1px solid var(--rule);border-radius:12px;padding:1.6vh 1vw;text-align:center}
.tile .n{font:600 clamp(30px,3.6vw,52px)/1 var(--mono);letter-spacing:-.03em}
.tile .l{margin-top:.6vh;font-size:clamp(12px,1.05vw,15px);color:var(--ink-2)}
.tile .dot{display:inline-block;width:10px;height:10px;border-radius:50%;margin-right:6px;vertical-align:-1px}
.wait{text-align:center;padding:5vh 2vw}
.wait .big2{font-size:clamp(24px,3vw,40px);font-weight:800;margin:1.6vh 0 .6vh}

/* 진입 스태거 */
.rv{opacity:0;transform:translateY(12px);transition:opacity .45s ease,transform .45s ease}
.slide.active .rv{opacity:1;transform:none}

/* ── 크롬(진행바·카운터·힌트·노트) ── */
.bar{position:fixed;left:0;bottom:0;height:4px;background:var(--accent);width:0;transition:width .3s ease;z-index:5}
.cnt{position:fixed;right:18px;bottom:12px;font:600 12px var(--mono);color:var(--sub);z-index:5}
.hint{position:fixed;left:18px;bottom:10px;font-size:11.5px;color:var(--sub);opacity:.75;z-index:5}
.nav{position:fixed;top:0;bottom:0;width:10vw;z-index:4;cursor:pointer}
.nav.prev{left:0}.nav.next{right:0}
aside.notes{display:none}
#np{display:none;position:fixed;right:16px;top:16px;width:min(38vw,540px);background:#1C1916;color:#F5F1EA;padding:14px 18px;border-radius:12px;
  font-size:15.5px;line-height:1.55;z-index:6;box-shadow:0 12px 40px rgba(0,0,0,.35)}
#np .np-time{font:600 13px var(--mono);color:#FFB36B;margin-bottom:6px}
body.notes #np{display:block}

@media (prefers-reduced-motion: reduce){ .slide,.rv{transition:none!important;transform:none!important} }
@media print{
  body{overflow:visible}
  .slide{position:static;opacity:1;visibility:visible;transform:none;min-height:96vh;page-break-after:always;padding:24px}
  .rv{opacity:1;transform:none}
  .bar,.cnt,.hint,.nav,#np{display:none!important}
}
</style>
</head>
<body>

<!-- 1 ─ 기: 훅 + 사건 -->
<section class="slide" id="s1">
  <div class="kicker rv">한화 신입사원 AI 솔루션 챌린지 · 과제 03 고객 페인포인트 해결</div>
  <div class="cols w2">
    <div>
      <h1 class="rv">불꽃이 끝나면,<br><span class="cu" data-v="100">100</span>만 명이 움직인다</h1>
      <p class="sub rv" style="font-size:clamp(16px,1.7vw,22px);max-width:44ch">서울세계불꽃축제 귀가 출구 내비 — "지금 어디로 나가야 덜 막히나"를 3초 안에.</p>
      <p class="src rv">박재형 · 행사일 2026-09-05(토) · <span class="mono">jaefit.github.io/hanwha-ai-challenge</span></p>
    </div>
    <div class="card rv">
      <div class="big">≈ 0<small>명</small></div>
      <h3>축제일 저녁 여의나루역 하차 — 역이 닫혀 있었다</h3>
      <ul class="pts" style="font-size:.92em">
        <li>공원에서 제일 가까운 역이라 지도앱은 오늘도 여의나루로 안내한다</li>
        <li>축제일 저녁엔 안전 통제로 <b>무정차</b> — 걸어가 봐야 닫혀 있다</li>
        <li>서울교통공사 실측(OA-12921), 2024·2025 두 해 공통 · 2026 공지 20:40~21:40 통제</li>
      </ul>
    </div>
  </div>
  <aside class="notes"></aside>
</section>

<!-- 2 ─ 기: 무엇이 빠져 있나 -->
<section class="slide" id="s2">
  <div class="kicker rv">문제 — 무엇이 빠져 있나</div>
  <h2 class="rv">지도앱도 서울시도 <em>상태</em>만 준다</h2>
  <div class="cols">
    <div class="card rv"><h3>지도앱</h3><div class="q">"가장 가까운 역"</div><p>통제를 모른다. 축제일 저녁에도 여의나루로 안내한다 — 막힌 길로 가는 안내.</p></div>
    <div class="card rv"><h3>서울시 실시간 도시데이터</h3><div class="q">"붐빔"</div><p>4단계 상태. 지금 붐빈다는 건 알지만 <b>어디로 가라</b>는 없다.</p></div>
    <div class="card rv" style="border-color:var(--accent)"><h3>빠진 것</h3><div class="q">"어디로 나가라"</div><p>출구 7개 중 지금·30분 뒤 덜 막히는 곳. 이걸 만든다.</p></div>
  </div>
  <p class="src rv">통제를 피한 인파가 옆 역으로 — 여의도역 21시 승차는 평시 토요일의 12~17배. 관람객 최다 집단은 20·30대 여성(KT), 어두운 공원과 통신 폭주 속에서 판단한다.</p>
  <aside class="notes"></aside>
</section>

<!-- 3 ─ 승: 관람객 화면 (실물) -->
<section class="slide" id="s3">
  <div class="kicker rv">해법 — 관람객 화면 (실제 배포본)</div>
  <h2 class="rv">상태가 아니라 <em>행동</em> — 묻지 않는 귀가 내비</h2>
  <div class="cols w2">
    <figure class="embed phone rv">
      <div class="frame"><div class="screen">
        <iframe src="go.html" title="관람객 화면 go.html"></iframe>
        <img class="fb" data-src="deck/fallback_go.png" alt="관람객 화면 캡처" hidden>
        <div class="fbmsg" hidden>라이브 화면 — 통신 필요</div>
      </div></div>
    </figure>
    <div>
      <ul class="pts">
        <li class="rv"><b>위치 자동</b> — 묻지 않는다. 권한이 없으면 이벤트광장 기준, 핀을 끌어 옮긴다</li>
        <li class="rv"><b>1위 자동</b> — 도보 + 예상 대기가 가장 짧은 출구가 맨 위. 등급 4단계 · 대기 분</li>
        <li class="rv"><b>시각 = 지금</b> — 차트를 문지르면 19~23시 예측으로 옮겨 간다</li>
        <li class="rv">서버 없는 정적 페이지 — 링크 하나, 통신 폭주에도 <b>기기 저장 폴백</b></li>
      </ul>
      <p class="src rv">실제 배포본을 그대로 띄웠다 (<span class="mono">…/go.html</span>) · 위치는 이벤트광장 기준 가상 위치</p>
    </div>
  </div>
  <aside class="notes"></aside>
</section>

<!-- 4 ─ 승: 운영 화면 + 3층 -->
<section class="slide" id="s4">
  <div class="kicker rv">운영 화면 — 그리고 그 아래 3층</div>
  <h2 class="rv">아래층이 죽어도 <em>위층이 산다</em></h2>
  <div class="cols w2">
    <figure class="embed ops rv">
      <div class="frame"><div class="screen">
        <iframe src="index.html" title="운영 화면 index.html"></iframe>
        <img class="fb" data-src="deck/fallback_ops.png" alt="운영 화면 캡처" hidden>
        <div class="fbmsg" hidden>라이브 화면 — 통신 필요</div>
      </div></div>
    </figure>
    <div class="stack">
      <div class="lay rv"><b>화면층</b><span>정적 웹페이지 — LIVE / 오래됨 / 사전표 자동 전환 + 기기 저장 오프라인 폴백</span></div>
      <div class="arr rv">▲ 5분마다 발행 (끊겨도 아래가 받친다)</div>
      <div class="lay rv"><b>당일층</b><span>도시데이터 API 5분 + CCTV 23대 60초 → α 보정 · 통제 이월 · 보행 지연 → 자동 발행</span></div>
      <div class="arr rv">▲ 규모만 보정 (없어도 서비스 성립)</div>
      <div class="lay rv"><b>사전층</b><span>공공 실측 7종 → 예측 엔진 + 백테스트 — 행사 전에 완결</span></div>
      <p class="src rv">다음 다섯 장이 층마다 "무엇을 · 어떻게 · 근거"다. 전 데이터 공개·합법 소스 · CCTV 는 프레임 미저장(집계만)</p>
    </div>
  </div>
  <aside class="notes"></aside>
</section>

<!-- 5 ─ 전: 사전층 ① 수요식 -->
<section class="slide" id="s5">
  <div class="kicker rv">사전층 ① — 왜 머신러닝이 아닌가</div>
  <h2 class="rv">수요 = <em>α</em> × E<sub>st</sub> × 도착형태</h2>
  <div class="card formula rv">\[ D_{s,h} \;=\; \alpha \cdot E_s \cdot f_s(h) \]</div>
  <div class="cols" style="margin-top:1.4vh">
    <div class="rv"><b>E<sub>st</sub> — 실측 뼈대</b><p class="sub" style="margin:.3vh 0 0;font-size:.9em">작년에 실제로 그 역으로 나간 인원(초과 승차). 비중까지 관측값.</p></div>
    <div class="rv"><b>f — 도착형태(물리)</b><p class="sub" style="margin:.3vh 0 0;font-size:.9em">쇼 종료 앵커 + 거리÷보행속도(밀도 반영) 지연.</p></div>
    <div class="rv"><b>α — 당일 배율</b><p class="sub" style="margin:.3vh 0 0;font-size:.9em">오늘이 예년의 몇 배인지, 5분마다 관측으로 갱신.</p></div>
  </div>
  <div class="cols" style="margin-top:1.2vh">
    <figure class="fig mini rv" data-chart="exits"><canvas></canvas></figure>
    <pre class="strip rv" data-strip="demand"></pre>
  </div>
  <p class="src rv">학습 표본이 축제일 단 2일 → ML 은 외우기가 된다. 실측 뼈대 + 물리 구조 모델 — 애틀랜타 MARTA 경기장 수요 예측(arXiv:2106.05359)과 같은 접근. 파라미터 6개, 전부 출처가 있다. 왼쪽 막대 = 출구 7개 실측 비중(2년 평균).</p>
  <aside class="notes"></aside>
</section>

<!-- 6 ─ 전: 사전층 ② 피더 -->
<section class="slide" id="s6">
  <div class="kicker rv">사전층 ② — 사람들은 어디서 타고 오나</div>
  <h2 class="rv">피더역 12곳이 여의도보다 <em>1시간 먼저</em> 움직인다</h2>
  <div class="cols w2">
    <figure class="fig rv" data-chart="radial"><div class="radial"></div></figure>
    <div>
      <figure class="fig mini rv" data-chart="feeder"><canvas></canvas></figure>
      <div class="lg rv">
        <span><i style="background:var(--c1)"></i>피더 12역 승차</span>
        <span><i style="background:var(--c2)"></i>여의도 하차 초과</span>
        <span>상관계수 <b class="mono" data-feeder-r>—</b></span>
      </div>
      <ol class="steps rv" style="margin-top:1.2vh;font-size:.92em">
        <li><b>초과 승차</b> — 서울 253역, 축제일 승차 − 평시 토요일 중앙값</li>
        <li><b>2년 필터</b> — 2024·2025 둘 다 튄 역만 인정</li>
        <li><b>KT 상한</b> — 휴대전화 이동 데이터의 구(區) 총량을 넘지 않게 배분</li>
        <li><b>교차 검증</b> — 배분 합이 여의도 도착과 규모·시간 모양이 맞는가</li>
      </ol>
    </div>
  </div>
  <p class="src rv">왼쪽 도식: 선 방향 = 실제 방위, 중심에서 거리 = 지하철 소요시간(추정, 링 10·20·30분), 원 크기 = 2년 귀속 인원. 오른쪽: 1시간 밀면 r 0.72 → 0.98 (2025) · 두 해 통합 0.988. 당일엔 12곳을 5분마다 감시 — 여의도가 붐비기 30~40분 전 조기 신호. 검증 전엔 예측에 안 넣는다.</p>
  <aside class="notes"></aside>
</section>

<!-- 7 ─ 전: 당일층 -->
<section class="slide" id="s7">
  <div class="kicker rv">당일층 — 5분마다 오늘 규모를 다시 잰다</div>
  <h2 class="rv">확신이 아니라 <em>범위</em>를 갱신한다</h2>
  <div class="cols">
    <div class="card rv">
      <h3>① α — 베이즈 격자</h3>
      <figure class="fig mini" data-chart="alpha"><canvas></canvas></figure>
      <div class="lg" style="font-size:clamp(10px,.85vw,12px)">
        <span><i style="background:var(--sub)"></i>사전 <b class="mono">1.00 [0.73~1.38]</b></span>
        <span><i style="background:var(--c1)"></i>사후 <b class="mono" data-alpha-now>—</b></span>
      </div>
      <p><span class="tag warn">관측 y 는 α=1.15 가정 · 사전·격자·σ 는 코드 값</span></p>
    </div>
    <div class="card rv">
      <h3>② CCTV 23대 → 보행속도</h3>
      <p style="text-align:center;margin:1vh 0">\( v(\rho)=\max\!\big(0.15,\;1.34\,[1-e^{-1.913(1/\rho-1/5.4)}]\big) \)</p>
      <table style="font-size:.85em">
        <tr><th>등급</th><th class="num">명/m²</th><th class="num">m/분</th></tr>
        <tr><td><span class="grade g1">여유</span></td><td class="num">1.5</td><td class="num">80</td></tr>
        <tr><td><span class="grade g2">주의</span></td><td class="num">3.0</td><td class="num">42</td></tr>
        <tr><td><span class="grade g3">경계</span></td><td class="num">4.0</td><td class="num">20</td></tr>
        <tr><td><span class="grade g4">심각</span></td><td class="num">5.0</td><td class="num">9</td></tr>
      </table>
      <p>60초마다 프레임을 집계만 하고 버린다 · DM-Count 군중 계수 · Kladek 식(자유보행 1.34 m/s, 정체 5.4명/m²)</p>
    </div>
    <div class="card rv">
      <h3>③ 워치독 · 발행</h3>
      <ul class="pts" style="font-size:.9em">
        <li>30초마다 프로세스 3개 생존 확인 → 죽으면 재기동(실측 17초)</li>
        <li><span class="mono">api_last_ok</span> 표식이 10분 넘게 안 바뀌면 경고 — 오류 레코드로는 못 속인다</li>
        <li>5분 발행 · 내용 같으면 커밋 안 함 · push 실패 시 rebase 재시도</li>
        <li>화면엔 "수집 끊김 · 마지막 수신 N분 전"</li>
      </ul>
    </div>
  </div>
  <pre class="strip rv" data-strip="alpha"></pre>
  <p class="src rv">격자 61점(1/3~3) 로그 등간격, 사전 LogNormal(0, 0.25²). 학습이 아니라 갱신이라 축제일 2일로 성립한다. 밴드가 좁아지는 속도가 빠른 건 열린 결정(C1). 보행속도 표의 m/분은 위 식을 등급 대표 밀도에 넣은 값.</p>
  <aside class="notes"></aside>
</section>

<!-- 8 ─ 전: 화면층 -->
<section class="slide" id="s8">
  <div class="kicker rv">화면층 — 혼잡장 위에서 길을 고른다</div>
  <h2 class="rv">못 본 곳은 <em>모른다고</em>, 길은 걸을 수 있는 길로</h2>
  <div class="cols">
    <div>
      <figure class="fig rv" data-chart="field"><canvas></canvas></figure>
      <div class="lg rv">
        <span><i style="background:var(--g1)"></i>여유</span><span><i style="background:var(--g2)"></i>주의</span>
        <span><i style="background:var(--g3)"></i>경계</span><span><i style="background:var(--g4)"></i>심각</span>
        <span><b class="mono" data-field-n>—</b></span>
        <span class="tag warn">등급 배치는 예시 · 좌표·커널·σ·GP 식은 실제 값</span>
      </div>
    </div>
    <div>
      <figure class="fig duo rv" data-chart="route"><canvas data-mode="shortest"></canvas><canvas data-mode="avoiding"></canvas></figure>
      <div class="lg rv">
        <span><i style="background:var(--c1)"></i>최단 <b class="mono">1,471m · 57분</b></span>
        <span><i style="background:var(--c2)"></i>혼잡 회피 <b class="mono">1,677m · 22분</b></span>
        <span><i style="background:rgba(179,53,43,.45)"></i>혼잡 봉우리(가정)</span>
        <span>노드 <b class="mono">9,219</b> · 간선 <b class="mono">26,042</b> (OSM)</span>
      </div>
    </div>
  </div>
  <pre class="strip rv" data-strip="blend"></pre>
  <p class="src rv">가우시안 과정 회귀(커널 150m·1200m 이중). 관측 없는 곳은 배제가 아니라 <b>σ</b> 로 남긴다 — 흐릴수록 근거가 얇은 곳. 이벤트광장→여의도역 직선 1,120m, 걸으면 <b>1,471m(1.31배)</b> — 모델의 우회계수 1.3 과 같다. 경로 비용은 거리가 아니라 <b>그 간선을 걷는 시간</b>이라 206m 더 걷고 35분 빠른 길이 이긴다. 반경 150m 봉우리는 가정.</p>
  <aside class="notes"></aside>
</section>

<!-- 9 ─ 전: 검증 -->
<section class="slide" id="s9">
  <div class="kicker rv">검증 — 맞혔는가, 그리고 깨지는가</div>
  <h2 class="rv">타임머신으로 맞히고, <em>우리 것을 먼저 깼다</em></h2>
  <div class="cols">
    <div>
      <figure class="fig rv" data-chart="backtest"><canvas></canvas></figure>
      <div class="lg rv">
        <span><i style="background:var(--c2)"></i>등급 적중</span>
        <span><i style="background:var(--c1)"></i>등급 불일치</span>
        <span><b class="mono" data-bt-hit>—</b> · 2025 교차연도 (B 모드)</span>
      </div>
    </div>
    <div>
      <figure class="fig rv" data-chart="redteam"><div class="rt tiles"></div></figure>
      <p class="rv" style="margin-top:1.4vh">조치는 <b>실패하는 테스트부터</b> — pytest 96건 상시 통과 · 다른 모델(Codex) 교차검증 포함 · 결함 대장 <span class="mono">redteam-20260901.md</span></p>
    </div>
  </div>
  <p class="src rv">한 해 데이터로 만든 모델이 다른 해를 맞히는가(B 모드) — 등급 적중 88%, 규모 오차 0.42/0.44. 오차가 남아도 화면이 말하는 단위는 등급이다. 오른쪽은 등급별 등재 건수 — 치명·높음은 전부 조치했고 보류·철회는 사유를 대장에 적었다.</p>
  <aside class="notes"></aside>
</section>

<!-- 10 ─ 결: 9/5 실전 -->
<section class="slide" id="s10">
  <div class="kicker rv">결과 — 9월 5일 실전</div>
  <h2 class="rv">실전으로 <em>채점받았다</em></h2>
  <figure class="fig rv" data-chart="live"><div class="live"></div></figure>
  <p class="src rv">우리 예측 vs 실측 등급 적중 · α 궤적 · 발행 틱 · 재기동 — <span class="mono">evaluate.py</span> 가 만든 숫자만 싣는다. 우천 연기면 전야제 리허설 데이터로 대체.</p>
  <aside class="notes"></aside>
</section>

<!-- 11 ─ 결: AI 활용 + 확장 -->
<section class="slide" id="s11">
  <div class="kicker rv">AI 도구 활용 — 그리고 확장</div>
  <h2 class="rv">판단은 사람이, <em>구현은 AI가</em></h2>
  <div class="cols w2">
    <div><ul class="pts">
      <li class="rv"><b>데이터 파이프라인</b> — 공공 데이터 7종 수집·정제·검증</li>
      <li class="rv"><b>예측 엔진</b> — 구조 모델 + 베이즈 α + 이월 규칙</li>
      <li class="rv"><b>수집기 2종</b> — 도시데이터 5분 · CCTV 60초 (DM-Count)</li>
      <li class="rv"><b>검증 3층</b> — 백테스트 · pytest · 당일 채점기</li>
      <li class="rv"><b>산출물</b> — 대시보드 · 보고서 · 이 덱, 전부 코드로 생성·검증</li>
      <li class="rv"><b>과정 기록</b> — 실패·정정 포함 커밋 로그, 버린 결정 문서화</li>
    </ul></div>
    <div class="card rv">
      <h3>같은 뼈대로 다른 현장</h3>
      <p>실측 뼈대 + 당일 보정 + 검증 — 이 구조는 행사와 무관하다.</p>
      <p style="margin-top:1vh"><b>한화 적용안</b> — 오렌지세이프티(주최측 안전 관제)에 <b>관람객 귀가 레이어</b>를 얹는다. 행사 주최사가 관제와 관람객 안내를 한 데이터로 본다.</p>
    </div>
  </div>
  <p class="src rv">프롬프트·명령·실패·패치 기록: 저장소 문서 + 커밋 로그 (Claude Code)</p>
  <aside class="notes"></aside>
</section>

<!-- 12 ─ 결: 클로징 -->
<section class="slide" id="s12">
  <div class="kicker rv">클로징</div>
  <h1 class="rv" style="font-size:clamp(32px,4.8vw,64px)">예측은 <em>검증까지</em>가 제품이다</h1>
  <p class="sub rv" style="font-size:clamp(16px,1.7vw,22px);max-width:52ch">실측을 뼈대로, 불확실성은 밴드로, 당일 실전으로 채점 — 끝까지 숫자로 말한다.</p>
  <p class="src rv" style="font-size:clamp(13px,1.2vw,16px)">
    대시보드 <span class="mono">jaefit.github.io/hanwha-ai-challenge</span> ·
    팀 보고서 <span class="mono">…/report.html</span> ·
    저장소 <span class="mono">github.com/jaefit/hanwha-ai-challenge</span>
  </p>
  <aside class="notes"></aside>
</section>

<div class="bar" id="bar"></div>
<div class="cnt" id="cnt"></div>
<div class="hint">← → 이동 · n 노트 · t 시계 리셋</div>
<div class="nav prev" aria-hidden="true"></div>
<div class="nav next" aria-hidden="true"></div>
<div id="np"><div class="np-time">00:00</div><div class="np-text"></div></div>

<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js"></script>
<script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/contrib/auto-render.min.js" onload="renderMathInElement(document.body,{delimiters:[{left:'\\[',right:'\\]',display:true},{left:'\\(',right:'\\)',display:false}],throwOnError:false})"></script>
<script>
(function () {
"use strict";
var slides = [].slice.call(document.querySelectorAll(".slide"));
var ACT = ["기", "기", "승", "승", "전", "전", "전", "전", "전", "결", "결", "결"];
var bar = document.getElementById("bar"), cnt = document.getElementById("cnt");
var cur = -1;

function go(i) {
  i = Math.max(0, Math.min(slides.length - 1, i));
  if (i === cur) return;
  cur = i;
  slides.forEach(function (s, k) { s.classList.toggle("active", k === i); });
  [].slice.call(slides[i].querySelectorAll(".rv")).forEach(function (el, k) {
    el.style.transitionDelay = (0.1 + k * 0.07) + "s";
  });
  countUp(slides[i]);
  playChart(slides[i]);
  fitEmbeds();
  renderNotes();
  bar.style.width = ((i + 1) / slides.length * 100) + "%";
  cnt.textContent = (i + 1) + " / " + slides.length + " · " + ACT[i];
  if (history.replaceState) history.replaceState(null, "", "#" + (i + 1));
}
function step(d) { go(cur + d); }

/* 큰 숫자 카운트업 — 진입마다 재생 */
var CU_REDUCE = window.matchMedia && matchMedia("(prefers-reduced-motion: reduce)").matches;
function countUp(slide) {
  [].slice.call(slide.querySelectorAll(".cu")).forEach(function (el) {
    var v = parseFloat(el.getAttribute("data-v"));
    var d = +(el.getAttribute("data-d") || 0);
    if (CU_REDUCE) { el.textContent = v.toFixed(d); return; }
    if (el.__raf) cancelAnimationFrame(el.__raf);
    var t0 = null;
    function tickcu(ts) {
      if (t0 === null) t0 = ts;
      var p = Math.min(1, (ts - t0) / 1800);
      p = 1 - Math.pow(1 - p, 3);
      el.textContent = (v * p).toFixed(d);
      el.__raf = p < 1 ? requestAnimationFrame(tickcu) : null;
    }
    el.__raf = requestAnimationFrame(tickcu);
  });
}

/* 발표자 노트 — n 으로 토글, t 로 시계 리셋 */
var T0 = Date.now();
function renderNotes() {
  var a = slides[cur].querySelector("aside.notes");
  document.querySelector("#np .np-text").textContent = a ? a.textContent.trim() : "";
}
setInterval(function () {
  var t = Math.floor((Date.now() - T0) / 1000);
  document.querySelector("#np .np-time").textContent = ("0" + Math.floor(t / 60)).slice(-2) + ":" + ("0" + (t % 60)).slice(-2) + "  /  05:00";
}, 1000);

document.addEventListener("keydown", function (e) {
  if (e.key === "ArrowRight" || e.key === " " || e.key === "PageDown") { e.preventDefault(); step(1); }
  else if (e.key === "ArrowLeft" || e.key === "PageUp") { e.preventDefault(); step(-1); }
  else if (e.key === "Home") go(0);
  else if (e.key === "End") go(slides.length - 1);
  else if (e.key === "n") document.body.classList.toggle("notes");
  else if (e.key === "t") T0 = Date.now();
});
document.querySelector(".nav.prev").addEventListener("click", function () { step(-1); });
document.querySelector(".nav.next").addEventListener("click", function () { step(1); });
var tx = null;
document.addEventListener("touchstart", function (e) { tx = e.touches[0].clientX; }, { passive: true });
document.addEventListener("touchend", function (e) {
  if (tx === null) return;
  var dx = e.changedTouches[0].clientX - tx; tx = null;
  if (Math.abs(dx) > 40) step(dx < 0 ? 1 : -1);
}, { passive: true });
window.addEventListener("hashchange", function () {
  var n = parseInt(location.hash.slice(1), 10);
  if (n >= 1 && n <= slides.length) go(n - 1);
});
window.addEventListener("beforeprint", function () {
  slides.forEach(function (s) {
    [].slice.call(s.querySelectorAll(".rv")).forEach(function (el) { el.style.transitionDelay = "0s"; });
    [].slice.call(s.querySelectorAll(".cu")).forEach(function (el) {
      if (el.__raf) cancelAnimationFrame(el.__raf);
      el.textContent = parseFloat(el.getAttribute("data-v")).toFixed(+(el.getAttribute("data-d") || 0));
    });
  });
});

/* ── 실물 임베드: 화면 크기에 맞춰 scale · 오프라인/지연이면 폴백 캡처 ── */
function fitEmbeds() {
  [].slice.call(document.querySelectorAll(".embed")).forEach(function (fig) {
    var frame = fig.querySelector(".frame"), screen = fig.querySelector(".screen");
    var W = fig.classList.contains("phone") ? 390 : 1280, H = fig.classList.contains("phone") ? 844 : 800;
    var r = fig.getBoundingClientRect();
    if (!r.width || !r.height) return;
    var border = fig.classList.contains("phone") ? 12 : 2;
    var s = Math.min((r.width - border) / W, (r.height - border) / H);
    screen.style.transform = "scale(" + s + ")";
    frame.style.width = (W * s + border) + "px";
    frame.style.height = (H * s + border) + "px";
  });
}
function watchEmbed(fig) {
  var ifr = fig.querySelector("iframe"), img = fig.querySelector("img.fb"), msg = fig.querySelector(".fbmsg"), ok = false;
  function fallback() {
    if (ok) return;
    ifr.hidden = true;
    img.onerror = function () { img.hidden = true; msg.hidden = false; };
    img.onload = function () { img.hidden = false; };
    img.src = img.getAttribute("data-src");
  }
  if (!navigator.onLine) { fallback(); return; }
  ifr.addEventListener("load", function () { ok = true; });
  setTimeout(function () { if (!ok) fallback(); }, 8000);   // 발표장 통신이 느리면 8초 뒤 캡처로
}
[].slice.call(document.querySelectorAll(".embed")).forEach(watchEmbed);

/* ── 코드 스트립 — docs/deck/code_strips.json (소스에서 잘라 온 것) ── */
function loadStrips() {
  var pres = [].slice.call(document.querySelectorAll("pre[data-strip]"));
  if (!pres.length) return;
  var esc = function (t) { return String(t).replace(/&/g, "&amp;").replace(/</g, "&lt;"); };
  fetch("deck/code_strips.json").then(function (r) { return r.json(); }).then(function (list) {
    var by = {}; list.forEach(function (s) { by[s.id] = s; });
    pres.forEach(function (pre) {
      var s = by[pre.getAttribute("data-strip")]; if (!s) return;
      pre.innerHTML = '<span class="ln">' + esc(s.file + ":" + s.start) + "   " + esc(s.caption) + "</span>" + s.lines.map(esc).join("\n");
    });
  }).catch(function () { pres.forEach(function (p) { p.hidden = true; }); });
}
loadStrips();

/* ── 차트 공통 ── 전부 docs/deck/*.json 에서 읽는다. 여기서 숫자를 다시 계산하지 않는다. */
var CHART_CACHE = {}, CHART_REDUCE = CU_REDUCE;
function chartData(name) {
  if (!CHART_CACHE[name]) CHART_CACHE[name] = fetch("deck/" + name + ".json").then(function (r) { return r.json(); });
  return CHART_CACHE[name];
}
function cssVar(k) { return getComputedStyle(document.documentElement).getPropertyValue(k).trim(); }
function hex(s) { s = s.replace("#", ""); return [parseInt(s.slice(0, 2), 16), parseInt(s.slice(2, 4), 16), parseInt(s.slice(4, 6), 16)]; }
function fitCanvas(cv) {
  var r = cv.getBoundingClientRect(), d = window.devicePixelRatio || 1;
  cv.width = Math.max(1, Math.round(r.width * d));
  cv.height = Math.max(1, Math.round(r.height * d));
  var x = cv.getContext("2d");
  x.setTransform(d, 0, 0, d, 0, 0);
  x.clearRect(0, 0, r.width, r.height);
  return { ctx: x, w: r.width, h: r.height };
}
function animate(cv, ms, draw) {
  if (cv.__raf) cancelAnimationFrame(cv.__raf);
  if (CHART_REDUCE) { draw(1); return; }
  var t0 = null;
  function tick(ts) {
    if (t0 === null) t0 = ts;
    var p = Math.min(1, (ts - t0) / ms);
    draw(1 - Math.pow(1 - p, 3));
    cv.__raf = p < 1 ? requestAnimationFrame(tick) : null;
  }
  cv.__raf = requestAnimationFrame(tick);
}
function axes(c, w, h, pad) {
  c.strokeStyle = cssVar("--rule"); c.lineWidth = 1;
  c.beginPath(); c.moveTo(pad.l, pad.t); c.lineTo(pad.l, h - pad.b); c.lineTo(w - pad.r, h - pad.b); c.stroke();
}
function label(c, txt, x, y, col, align, size, font) {
  c.fillStyle = col; c.textAlign = align || "left"; c.textBaseline = "middle";
  c.font = (size || 11) + "px " + (font === "sans" ? "'Pretendard Variable', Pretendard, sans-serif" : "'IBM Plex Mono', ui-monospace, monospace");
  c.fillText(txt, x, y);
}

var CHARTS = {};
/* charts:functions */

function playChart(slide) {
  [].slice.call(slide.querySelectorAll("[data-chart]")).forEach(function (fig) {
    var spec = CHARTS[fig.getAttribute("data-chart")];
    if (!spec) return;
    var cv = fig.querySelector("canvas");
    chartData(spec[0]).then(function (d) { spec[1](cv, d, slide, fig); })
      .catch(function () { fig.innerHTML = '<p class="src">그래프 데이터를 받지 못했다.</p>'; });
  });
}
var rzT = null;
window.addEventListener("resize", function () {
  clearTimeout(rzT);
  rzT = setTimeout(function () { fitEmbeds(); if (cur >= 0) playChart(slides[cur]); }, 180);
});

var n0 = parseInt(location.hash.slice(1), 10);
go((n0 >= 1 && n0 <= slides.length) ? n0 - 1 : 0);
})();
</script>
</body>
</html>
```

- [ ] **Step 4: 통과 확인**

Run: `.venv/bin/python -m pytest tests/test_deck.py -q`
Expected: 18 passed (기존 9 + Task 1 의 5 + 구조 4). 특히 `test_route_numbers_in_copy_match_export`(1,471m · 1,120m · 1.31배 · 9,219 · 26,042) · `test_feeder_correlation…`(`0.72 → 0.98` · `0.988`) · `test_backtest_hit_rate…`(`88%`) · `test_synthetic_layers…`(`α=1.15 가정` · `등급 배치는 예시`) 가 새 카피로 통과해야 한다. 하나라도 깨지면 카피의 숫자가 아니라 **데이터가 바뀐 것**이니 `docs/deck/*.json` 값을 보고 카피를 맞춘다.

- [ ] **Step 5: 브라우저에서 눈으로 — 로컬 서버**

```bash
cd docs && (python3 -m http.server 8765 > /tmp/deck_http.log 2>&1 &) && sleep 1 && open http://localhost:8765/deck.html
```
확인: 12장 넘김(← →) · 하단 진행바·`3 / 12 · 승` · `n` 으로 노트 패널(아직 빈 문장) · 3장 폰 프레임에 go.html 이 뜨고 위치 팝업이 **안 뜬다** · 4장 운영 화면 축소 · 5장 수식 렌더 · 5·7·8장 코드 스트립에 소스가 들어온다. 차트 자리는 아직 빈 canvas — 정상.

- [ ] **Step 6: 커밋**

```bash
git add docs/deck.html tests/test_deck.py
git commit -m "deck: v2 뼈대 — 라이트 테마·12장 카피·실물 임베드·코드 스트립·발표자 노트 (차트는 다음 커밋)"
```

---

### Task 3: 차트 A — 출구 비중 막대(5장) · 피더 방사형 + lag 미니(6장)

**Files:**
- Modify: `docs/deck.html` — `/* charts:functions */` 줄 **앞**에 함수 삽입, 그 줄 뒤에 `CHARTS.*` 등록은 함수 끝에 같이 넣는다
- Test: 이 태스크는 그림이라 정적 테스트가 없다. 확인은 브라우저(Step 4). 회귀 가드는 Task 5 의 키 집합 테스트

**Interfaces:**
- Consumes: `animate` · `fitCanvas` · `label` · `cssVar` · `CHART_REDUCE` (Task 2)
- Produces: `CHARTS.exits` · `CHARTS.radial` · `CHARTS.feeder`

- [ ] **Step 1: 함수 3개 삽입 — `/* charts:functions */` 바로 앞에**

```js
/* ① 출구 7개 실측 비중 — 가로 막대, 비중 내림차순 (exit_bars.json) */
function roundRect(c, x, y, w, h, r) {
  r = Math.min(r, h / 2, Math.max(0, w) / 2);
  c.beginPath(); c.moveTo(x, y); c.lineTo(x + w - r, y); c.quadraticCurveTo(x + w, y, x + w, y + r);
  c.lineTo(x + w, y + h - r); c.quadraticCurveTo(x + w, y + h, x + w - r, y + h); c.lineTo(x, y + h); c.closePath();
}
function drawExits(cv, d) {
  var rows = d.exits, max = rows[0].share;
  animate(cv, 1600, function (p) {
    var out = fitCanvas(cv), c = out.ctx, w = out.w, h = out.h;
    var pad = { l: Math.min(150, w * 0.34), r: 54, t: 4, b: 4 }, rh = (h - pad.t - pad.b) / rows.length;
    rows.forEach(function (r, i) {
      var y = pad.t + rh * i + rh * 0.2, bh = rh * 0.6;
      var bw = (w - pad.l - pad.r) * (r.share / max) * p;
      label(c, r.name, pad.l - 10, y + bh / 2, cssVar("--ink-2"), "right", 12, "sans");
      c.fillStyle = cssVar("--c1"); roundRect(c, pad.l, y, bw, bh, 4); c.fill();
      label(c, Math.round(r.share * 100 * p) + "%", pad.l + bw + 8, y + bh / 2, cssVar("--ink"), "left", 12);
    });
  });
}
CHARTS.exits = ["exit_bars", drawExits];

/* ② 피더 12곳 방사형 — 방위 실제, 거리 = 소요시간, 원 = 귀속 인원 (feeder_map.json). 보고서 그림 1 과 같은 규칙 */
function drawRadial(cv, d, slide, fig) {
  var host = fig.querySelector(".radial"); host.innerHTML = "";
  var NS = "http://www.w3.org/2000/svg", S = 560, C = S / 2;
  var svg = document.createElementNS(NS, "svg"); svg.setAttribute("viewBox", "0 0 " + S + " " + S);
  function el(t, a, txt) {
    var e = document.createElementNS(NS, t);
    for (var k in a) e.setAttribute(k, a[k]);
    if (txt != null) e.textContent = txt;
    svg.appendChild(e); return e;
  }
  var R = function (min) { return 34 + min * 6.3; };        // 35분 → 254px, 링 30분 → 223px
  var items = [];
  d.rings_min.forEach(function (m, i) {
    items.push([el("circle", { cx: C, cy: C, r: R(m), fill: "none", stroke: cssVar("--rule"), "stroke-width": 1.2, "stroke-dasharray": "3 4" }), i * 110]);
    items.push([el("text", { x: C + 5, y: C - R(m) - 5, fill: cssVar("--sub"), "font-size": 11, "font-family": "IBM Plex Mono, monospace" }, m + "분"), i * 110 + 60]);
  });
  var maxP = d.feeders[0].persons;
  d.feeders.forEach(function (f, i) {
    var a = f.bearing_deg * Math.PI / 180, r = R(f.travel_min);
    var x = C + r * Math.sin(a), y = C - r * Math.cos(a);
    items.push([el("line", { x1: C, y1: C, x2: x, y2: y, stroke: cssVar("--c1"), "stroke-width": 1.4, "stroke-opacity": .5 }), 380 + i * 60]);
    items.push([el("circle", { cx: x, cy: y, r: 5 + Math.sqrt(f.persons / maxP) * 11, fill: cssVar("--c1"), "fill-opacity": .88, stroke: cssVar("--sheet"), "stroke-width": 2 }), 440 + i * 60]);
    var right = x >= C, tx = right ? x + 15 : x - 15, anchor = right ? "start" : "end";
    items.push([el("text", { x: tx, y: y - 3, fill: cssVar("--ink"), "font-size": 13, "font-weight": 700, "text-anchor": anchor }, f.name), 480 + i * 60]);
    items.push([el("text", { x: tx, y: y + 11, fill: cssVar("--sub"), "font-size": 10.5, "font-family": "IBM Plex Mono, monospace", "text-anchor": anchor },
      f.travel_min + "분 · " + f.persons.toLocaleString("en-US") + "명"), 480 + i * 60]);
  });
  el("circle", { cx: C, cy: C, r: 9, fill: cssVar("--ink") });
  el("text", { x: C, y: C + 27, fill: cssVar("--ink"), "font-size": 13, "font-weight": 800, "text-anchor": "middle" }, d.center.name);
  host.appendChild(svg);
  items.forEach(function (it) {
    it[0].style.opacity = CHART_REDUCE ? 1 : 0;
    if (!CHART_REDUCE) it[0].style.transition = "opacity .35s ease " + (it[1] / 1000) + "s";
  });
  if (!CHART_REDUCE) requestAnimationFrame(function () { requestAnimationFrame(function () { items.forEach(function (it) { it[0].style.opacity = 1; }); }); });
}
CHARTS.radial = ["feeder_map", drawRadial];
```

그리고 **피더 lag 미니**: `git show fba7bac:docs/deck.html | sed -n 632,661p` 로 `drawFeeder` 함수(주석 줄 631 포함해 631~661)를 그대로 가져와 같은 자리(`/* charts:functions */` 앞)에 붙이고, 바로 뒤에 한 줄:

```js
CHARTS.feeder = ["feeder_lag", drawFeeder];
```

`drawFeeder` 는 `cssVar("--c1")`·`--c2`·`--sub` 만 쓰므로 고칠 게 없다. `axes` 가 이제 `--rule` 로 선을 긋는다.

- [ ] **Step 2: 문법 확인**

Run: `node -e "const s=require('fs').readFileSync('docs/deck.html','utf8');const m=s.match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log('js ok')"`
Expected: `js ok`. (IIFE 를 실행하지 않고 파싱만 한다 — 문법 오류면 여기서 죽는다)

- [ ] **Step 3: 정적 테스트**

Run: `.venv/bin/python -m pytest tests/test_deck.py -q`
Expected: 18 passed.

- [ ] **Step 4: 브라우저 확인 (로컬 서버 Task 2 Step 5)**

`http://localhost:8765/deck.html#5` — 막대 7개가 비중 순으로 차오르고 오른쪽에 %. `#6` — 링 3개 → 선 12개 → 점·이름이 차례로 뜬다. 오른쪽 위 미니 차트에서 주황 곡선이 오른쪽으로 밀리며 `r 0.98…` 로 바뀐다. **라벨이 겹치면**(영등포시장·신길이 둘 다 3분이라 중심 가까이 붙는다) `R()` 의 `34` 를 `44` 로 올리고 다시 본다. 그림 판정은 헤드리스로 하지 않는다.

- [ ] **Step 5: 커밋**

```bash
git add docs/deck.html
git commit -m "deck: 5장 출구 비중 막대 · 6장 피더 방사형 + lag 미니"
```

---

### Task 4: 차트 B — α 격자(7장) · 혼잡장 + 경로 2패널(8장) — 옛 덱 이식 + 라이트 재도색

**Files:**
- Modify: `docs/deck.html` — `/* charts:functions */` 앞에 삽입

**Interfaces:**
- Consumes: Task 2 공통 함수 · `hex`
- Produces: `CHARTS.alpha` · `CHARTS.field` · `CHARTS.route`

- [ ] **Step 1: 옛 덱에서 잘라 온다**

```bash
git show fba7bac:docs/deck.html | sed -n 663,695p   > /tmp/deck_alpha.js     # drawAlpha
git show fba7bac:docs/deck.html | sed -n 736,755p   > /tmp/deck_apf.js       # APF_SD_MAX · APF_FEATHER · FIELD_STOPS · apfRamp · rampHex
git show fba7bac:docs/deck.html | sed -n 782,787p   > /tmp/deck_fcolor.js    # fieldColor (hex 는 Task 2 에 이미 있다 — 788줄은 제외)
git show fba7bac:docs/deck.html | sed -n 876,922p   > /tmp/deck_fcanvas.js   # drawFieldCanvas
git show fba7bac:docs/deck.html | sed -n 924,1040p  > /tmp/deck_route.js     # drawRoute · drawRouteOn
```

지도(MapLibre) 버전 `drawFieldMap`(789~874)은 **가져오지 않는다** — 발표장 통신에 매달리지 않고, H11 같은 타일 사고를 덱에서 없앤다. 혼잡장은 캔버스 렌더 하나로 간다.

- [ ] **Step 2: 다섯 조각을 `/* charts:functions */` 앞에 이 순서로 붙이고, 아래 치환을 정확 문자열로 적용한다**

붙이는 순서: `deck_apf.js` → `deck_fcolor.js` → `deck_alpha.js` → `deck_fcanvas.js` → `deck_route.js`. 그 뒤에 등록 3줄:

```js
CHARTS.alpha = ["alpha_grid", drawAlpha];
CHARTS.field = ["field_grid", drawFieldCanvas];
CHARTS.route = ["route_demo", drawRoute];
```

치환(각각 정확히 1회, `grep -c` 로 세고 바꾼다):

| 어디 | 옛 문자열 | 새 문자열 | 이유 |
|:--|:--|:--|:--|
| drawAlpha | `curve(prior, "#39404e", null);` | `curve(prior, cssVar("--sub"), null);` | 검정용 회색 |
| drawAlpha | `c.strokeStyle = cssVar("--accent"); c.lineWidth = 1.5;` | `c.strokeStyle = cssVar("--c1"); c.lineWidth = 1.5;` | 액센트는 도형·바에만 — 중앙값 선은 시리즈색 |
| drawFieldCanvas | `c.strokeStyle = cssVar("--bg"); c.lineWidth = 1.6; c.stroke();` | `c.strokeStyle = cssVar("--sheet"); c.lineWidth = 1.6; c.stroke();` | 관측점 테두리 |
| drawFieldCanvas | `c.fillStyle = "rgba(11,13,18,.72)";` | `c.fillStyle = "rgba(255,255,255,.88)";` | 지명 칩 바탕 |
| drawRouteOn | `var ph = Math.max(0, Math.min(1, p * 3 - idx));      // 이 패널의 진행도` | `var ph = Math.max(0, Math.min(1, p * 2 - idx));      // 이 패널의 진행도 (2패널)` | 3→2 패널 |
| drawRouteOn | `c.fillStyle = "#39404e";` | `c.fillStyle = "#C9C4BA";` | 탐색 점 |
| drawRouteOn | `c.fillStyle = "rgba(224,74,74,.30)"; c.fill();` | `c.fillStyle = "rgba(179,53,43,.22)"; c.fill();` | 봉우리 = g4 |
| drawRouteOn | `c.strokeStyle = "rgba(11,13,18,.85)"; c.lineWidth = wd + 3.3; c.stroke();` | `c.strokeStyle = "rgba(255,255,255,.92)"; c.lineWidth = wd + 3.3; c.stroke();` | 경로 후광 |
| drawRouteOn | `c.strokeStyle = cssVar("--bg"); c.lineWidth = 2; c.stroke();` (출발·도착 마커, 1회) | `c.strokeStyle = cssVar("--sheet"); c.lineWidth = 2; c.stroke();` | 마커 테두리 |
| drawRouteOn | `label(c, STAT[mode], 10, 31, mode === "both" ? cssVar("--accent2") : cssVar("--sub"), "left", 11);` | `label(c, STAT[mode], 10, 31, cssVar("--sub"), "left", 11);` | `--accent2` 없앰 |
| drawRoute | `chartData("field_grid").then(function (fd) { drawRouteOn(cv, d, fd); })` | 그대로 | `chartData` 는 Task 2 에 있다 |

`drawFieldCanvas(cv, d, slide)` 의 시그니처는 `(cv, d, slide, fig)` 호출과 호환된다(네 번째 인자 무시). `drawRoute(cv, d, slide)` 도 같다 — 안에서 `cv.parentNode` 로 figure 를 찾아 두 canvas 를 얻는다.

- [ ] **Step 3: 문법 + 정적 테스트**

Run: `node -e "const s=require('fs').readFileSync('docs/deck.html','utf8');const m=s.match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log('js ok')" && .venv/bin/python -m pytest tests/test_deck.py -q`
Expected: `js ok` · 18 passed.

- [ ] **Step 4: 브라우저 확인**

`#7` — 왼쪽 카드에서 종 모양이 좁아지며 `α 1.1… [ … ]` 갱신. `#8` — 왼쪽 혼잡장이 관측 8프레임으로 차오르고 흐린 곳이 남는다(`관측 N대 · 평균 σ`). 오른쪽 두 패널: ① 최단이 탐색 점을 뿌린 뒤 봉우리를 통과하며 천천히, ② 회피가 돌아서 먼저 도착. 제목 `① 최단 — 봉우리를 통과` / `② 혼잡 회피 — 돌아간다`.

- [ ] **Step 5: 커밋**

```bash
git add docs/deck.html
git commit -m "deck: 7장 α 격자 · 8장 혼잡장 + 경로 2패널 — 옛 덱 이식, 라이트 재도색"
```

---

### Task 5: 차트 C — 백테스트 산점 + 결함 타일(9장) · 실전 결과 자리(10장) · 키 집합 확정

**Files:**
- Modify: `docs/deck.html` — `/* charts:functions */` 앞에 삽입
- Modify: `tests/test_deck.py` — `test_every_chart_has_its_data_file` 에 한 줄 추가

**Interfaces:**
- Consumes: `countUp(slide)` (Task 2)
- Produces: `CHARTS.backtest` · `CHARTS.redteam` · `CHARTS.live`

- [ ] **Step 1: 테스트에 등호 한 줄 — `assert keys <= set(CHART_FILES), …` 바로 아래에**

```python
    assert keys == set(CHART_FILES), f"차트 9종이 전부 있어야 한다 — 빠진 것: {set(CHART_FILES) - keys}"
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/bin/python -m pytest tests/test_deck.py -q -k every_chart`
Expected: 통과한다 — 마크업의 `data-chart` 9종은 Task 2 에 다 있다. 이 assert 는 이후 누가 슬라이드를 지웠을 때를 위한 가드다.

- [ ] **Step 3: 백테스트 이식 + 신규 2개 삽입**

```bash
git show fba7bac:docs/deck.html | sed -n 697,730p > /tmp/deck_bt.js   # drawBacktest
```

`/* charts:functions */` 앞에 `deck_bt.js` 를 붙이고 치환 1건: `c.strokeStyle = cssVar("--bg"); c.lineWidth = 2; c.stroke();   // 겹칠 때 서로 떨어져 보이게` → `c.strokeStyle = cssVar("--sheet"); c.lineWidth = 2; c.stroke();   // 겹칠 때 서로 떨어져 보이게`. 그 뒤에:

```js
CHARTS.backtest = ["backtest_bars", drawBacktest];

/* ⑦ 결함 대장 — 등급별 등재 건수 타일 (redteam_counts.json, 대장 ID 를 센 값) */
function drawRedteam(cv, d, slide, fig) {
  var host = fig.querySelector(".rt"); host.innerHTML = "";
  var order = [["치명", "g4"], ["높음", "g3"], ["중간", "g2"], ["낮음", "g1"]];
  order.forEach(function (o) {
    var t = document.createElement("div"); t.className = "tile";
    t.innerHTML = '<div class="n"><span class="cu" data-v="' + d.by_grade[o[0]] + '">0</span></div>'
      + '<div class="l"><i class="dot ' + o[1] + '"></i>' + o[0] + '</div>';
    host.appendChild(t);
  });
  var s = document.createElement("p"); s.className = "src"; s.style.gridColumn = "1 / -1"; s.style.marginTop = "0";
  s.textContent = "등재 " + d.total + "건 · 철회 " + d.retracted.length + "건(" + d.retracted.join(", ") + ") · 5회차까지, 다른 모델 교차검증 포함";
  host.appendChild(s);
  countUp(slide);
}
CHARTS.redteam = ["redteam_counts", drawRedteam];

/* ⑧ 9/5 실전 결과 — 비어 있으면 자리, 채워지면 타일 (live_result.json) */
function drawLive(cv, d, slide, fig) {
  var host = fig.querySelector(".live"); host.innerHTML = "";
  if (!d.filled) {
    host.innerHTML = '<div class="card wait"><span class="pill off">결과 대기</span>'
      + '<div class="big2">9/5 실전 결과 — 9/6 채움</div>'
      + '<p class="sub">' + (d.note || "") + '</p></div>';
    return;
  }
  var tiles = [["등급 적중", d.grade_hit], ["α 최종", d.alpha_final], ["발행 틱", d.ticks], ["재기동", d.restarts]];
  host.innerHTML = '<div class="tiles">' + tiles.map(function (t) {
    return '<div class="tile"><div class="n" style="font-size:clamp(22px,2.6vw,38px)">' + t[1] + '</div><div class="l">' + t[0] + '</div></div>';
  }).join("") + '</div>' + (d.note ? '<p class="src">' + d.note + '</p>' : "");
}
CHARTS.live = ["live_result", drawLive];
```

`.dot.g1~g4` 배경은 Task 2 의 `.g1{background:…}` 규칙이 그대로 먹는다.

- [ ] **Step 4: 문법 + 전체 테스트**

Run: `node -e "const s=require('fs').readFileSync('docs/deck.html','utf8');const m=s.match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log('js ok')" && .venv/bin/python -m pytest tests -q`
Expected: `js ok` · **105 passed** (96 + 9). 다른 테스트 파일은 건드리지 않았으니 96 이 그대로여야 한다.

- [ ] **Step 5: 브라우저 확인**

`#9` — 산점이 점점 찍히고 `등급 적중 88%`, 오른쪽 타일 4개가 카운트업(치명 6 · 높음 10 · 중간 14 · 낮음 6, 2026-09-04 기준) + "등재 36건 · 철회 1건(H8)". `#10` — 빨간 필 「결과 대기」 + "9/5 실전 결과 — 9/6 채움".

- [ ] **Step 6: 커밋**

```bash
git add docs/deck.html tests/test_deck.py
git commit -m "deck: 9장 백테스트 산점 + 결함 타일 · 10장 실전 결과 자리 · 차트 9종 키 집합 고정"
```

---

### Task 6: 발표자 노트 12장 · README · 헤드리스 검증 · 마무리

**Files:**
- Modify: `docs/deck.html` — 12개 `<aside class="notes"></aside>` 에 대본
- Modify: `README.md:15,30` + 「발표」 절 추가
- Test: `tests/test_deck.py` 에 노트 길이 가드 1건

**Interfaces:** 없음 (내용·문서)

- [ ] **Step 1: 노트 길이 테스트 추가 — `tests/test_deck.py` 끝**

```python


def test_speaker_notes_fit_five_minutes(html):
    """장당 25초 — 한국어 발화 ≈ 분당 300자. 60~140자면 20~30초. 빈 노트는 발표 중 화면이 침묵한다."""
    notes = re.findall(r'<aside class="notes">(.*?)</aside>', html, re.S)
    assert len(notes) == 12
    for i, n in enumerate(notes, 1):
        t = re.sub(r"\s+", " ", n).strip()
        assert 60 <= len(t) <= 170, f"s{i} 노트 {len(t)}자 — 60~170자로"
```

- [ ] **Step 2: 실패 확인**

Run: `.venv/bin/python -m pytest tests/test_deck.py -q -k speaker_notes`
Expected: FAIL — `s1 노트 0자`.

- [ ] **Step 3: 대본 12개를 각 슬라이드의 `<aside class="notes"></aside>` 안에 넣는다** (id 순서대로, 문장 그대로)

- s1: `불꽃이 끝나는 순간 백만 명이 한꺼번에 움직입니다. 그런데 공원에서 제일 가까운 여의나루역은 그 시간에 닫혀 있습니다. 두 해 실측에서 하차가 0에 가깝습니다. 지도앱은 오늘도 그 역으로 안내합니다.`
- s2: `문제를 정확히 하겠습니다. 지도앱은 가장 가까운 역을 주고, 서울시는 붐빔이라는 상태를 줍니다. 둘 다 어디로 나가라는 말은 없습니다. 빠진 건 행동입니다. 그걸 만들었습니다.`
- s3: `실제 배포된 관람객 화면입니다. 묻지 않습니다. 위치는 자동, 지금 시각 기준으로 도보와 대기가 가장 짧은 출구가 맨 위에 옵니다. 서버가 없는 정적 페이지라 통신이 폭주해도 기기에 저장된 마지막 화면으로 버팁니다.`
- s4: `운영 화면과 그 아래 구조입니다. 사전층이 행사 전에 예측표를 완성하고, 당일층이 오늘 규모만 보정하고, 화면층이 보여줍니다. 아래층이 죽어도 위층이 삽니다. 이제 층마다 무엇을 어떻게 만들었는지 다섯 장으로 보겠습니다.`
- s5: `사전층의 식입니다. 수요는 당일 배율 알파 곱하기 작년 실측 출구 비중 곱하기 도착 형태. 학습 표본이 축제일 이틀뿐이라 머신러닝은 외우기가 됩니다. 실측을 뼈대로 쓰고 물리로 지연을 넣었습니다. 아래는 실제 코드입니다.`
- s6: `사람들이 어디서 타고 오는지 서울 전 역의 초과 승차로 역추적했습니다. 두 해 연속 튄 역만 남기고 KT 이동 데이터 총량으로 상한을 걸었습니다. 이 열두 역의 승차가 여의도 도착보다 한 시간 먼저 움직입니다. 상관 0.98입니다.`
- s7: `당일층입니다. 오늘이 예년의 몇 배인지 5분마다 베이즈 격자로 갱신합니다. 확신이 아니라 범위를 갱신합니다. CCTV 스물세 대는 프레임을 저장하지 않고 밀도만 세서 보행속도로 바꾸고, 워치독이 30초마다 프로세스를 지킵니다.`
- s8: `화면층입니다. 관측 서른 점을 가우시안 과정으로 한 장의 면으로 만들되, 못 본 곳은 모른다고 흐리게 남깁니다. 경로는 실제 보행망 위에서 거리가 아니라 걷는 시간으로 고릅니다. 이백 미터 더 걷고 삼십오 분 빠른 길이 이깁니다.`
- s9: `검증입니다. 한 해 데이터로 만든 모델이 다른 해를 맞히는지 봤고 등급 적중 88퍼센트입니다. 그리고 제출 전에 우리 것을 먼저 깼습니다. 다른 모델까지 붙여 결함 서른여섯 건을 등재하고 치명과 높음은 전부 조치했습니다.`
- s10: `9월 5일 실전 결과입니다. 예측과 실측의 등급 적중, 알파 궤적, 발행 틱과 재기동 횟수를 채점기가 만든 숫자 그대로 실었습니다. 예측은 검증까지가 제품이라는 말을 여기서 지킵니다.`
- s11: `판단은 사람이 하고 구현은 AI가 했습니다. 파이프라인, 엔진, 수집기, 검증, 이 덱까지 전부 코드로 만들고 검증했고, 실패와 정정도 커밋 로그에 남겼습니다. 같은 뼈대는 다른 행사에도 그대로 갑니다. 한화에서는 오렌지세이프티에 관람객 레이어를 얹는 그림입니다.`
- s12: `실측을 뼈대로, 불확실성은 밴드로, 당일 실전으로 채점했습니다. 예측은 검증까지가 제품입니다. 화면과 코드는 전부 링크에 있습니다. 감사합니다.`

- [ ] **Step 4: README 갱신**

`README.md` 15줄 `| **[피치 덱](https://jaefit.github.io/hanwha-ai-challenge/deck.html)** | 발표용 18장 | |` → `| **[피치 덱](https://jaefit.github.io/hanwha-ai-challenge/deck.html)** | 발표용 12장 · 5분 | |`
30줄 `| 피치 덱 | `docs/deck.html` | 발표용 18장 |` → `| 피치 덱 | `docs/deck.html` | 발표용 12장 · 5분 · 라이트 테마 (v2, 2026-09-04) |`

그리고 README 의 마지막 절 뒤에 추가:

```markdown

## 발표 (피치 덱)

- 덱 `docs/deck.html` — 12장, 5분. `← →` 이동, `n` 발표자 노트(장마다 25초 대본 + 경과 시계), `t` 시계 리셋.
- 3장·4장은 배포본 `go.html`·`index.html` 을 iframe 으로 그대로 띄운다. 위치 권한은 주지 않아 팝업이 없고 이벤트광장 기준 가상 위치로 뜬다.
- **통신이 없으면** 8초 뒤 `docs/deck/fallback_go.png`·`fallback_ops.png` 캡처로 바뀐다. 캡처는 실기기에서 직접 찍어 그 이름으로 넣는다(없으면 "라이브 화면 — 통신 필요" 문구).
- 발표장엔 노트북 지참 + 휴대폰 테더링. 덱 데이터(`docs/deck/*.json`)는 `.venv/bin/python tools/deck_data.py` 로 다시 만들고, 코드 스트립이 소스와 어긋나면 `tests/test_deck.py` 가 잡는다.
- 10장(실전 결과)은 `docs/deck/live_result.json` 을 채우면 자동으로 타일이 된다 — 9/6 `evaluate.py` 결과로.
```

- [ ] **Step 5: 전체 테스트**

Run: `.venv/bin/python -m pytest tests -q`
Expected: **106 passed**.

- [ ] **Step 6: 헤드리스 검증 — 12 섹션 · KaTeX 오류 0 · 콘솔 에러 0**

메모리 레시피대로 가상시간 + `--dump-dom`. 로컬 서버가 Task 2 Step 5 에서 떠 있어야 한다(없으면 다시 `cd docs && (python3 -m http.server 8765 > /tmp/deck_http.log 2>&1 &)`).

```bash
CH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
"$CH" --headless=new --disable-gpu --window-size=1440,900 --virtual-time-budget=12000 --enable-logging=stderr --v=0 \
  --dump-dom "http://localhost:8765/deck.html#5" > /tmp/deck_dom.html 2> /tmp/deck_console.log
python3 - <<'PY'
import re
h=open('/tmp/deck_dom.html',encoding='utf-8').read()
print("sections", len(re.findall(r'<section class="slide', h)))
print("katex", len(re.findall(r'class="katex"', h)), "errors", len(re.findall(r'katex-error', h)))
print("strips filled", len(re.findall(r'<pre class="strip[^"]*"[^>]*><span class="ln">', h)))
c=open('/tmp/deck_console.log',encoding='utf-8',errors='ignore').read()
bad=[l for l in c.splitlines() if 'CONSOLE' in l and ('Uncaught' in l or 'ERROR' in l or 'Failed to load' in l)]
print("console errors", len(bad)); print("\n".join(bad[:8]))
PY
```
Expected: `sections 12` · `katex 2 errors 0`(5장 수식 1 + 7장 인라인 1 — 활성 슬라이드 무관하게 auto-render 가 문서 전체를 돈다) · `strips filled 3` · `console errors 0`. 외부 폰트·CDN 차단 환경이면 `Failed to load` 가 폰트 URL 로 뜰 수 있다 — 그건 무시하고 `Uncaught` 만 본다. iframe 안 지도(WebGL)는 헤드리스에서 못 본다 — Step 7.

- [ ] **Step 7: 실기기 확인 (사람) — 스펙 DoD 3**

발표 PC 브라우저에서 `https://jaefit.github.io/hanwha-ai-challenge/deck.html` (수집기의 다음 push 후 1~2분) 또는 로컬 서버:
1. 3장 — 폰 프레임 안에 go.html 지도 바닥·시트·순위가 뜨고 위치 팝업이 **없다**. 4장 — 운영 화면이 축소되어 뜬다
2. 브라우저 오프라인(개발자 도구 Network → Offline)으로 바꾸고 새로고침 → 8초 안에 폴백 문구 또는 캡처
3. `n` 노트 켜고 처음부터 끝까지 소리 내어 읽는다 — 시계가 **5:00 ± 0:30** 에 12장 도착
4. 폴백 캡처 2장은 이때 찍어 `docs/deck/fallback_go.png`·`fallback_ops.png` 로 넣고 커밋(사용자 몫)

- [ ] **Step 8: 커밋 + push 확인**

```bash
git add docs/deck.html README.md tests/test_deck.py
git commit -m "deck: 발표자 노트 12장(25초 대본) · README 발표 절 · 노트 길이 가드"
git pull --rebase --autostash -q origin main && git push -q origin main
git fetch -q origin && git status -sb | head -1     # "## main...origin/main" 이고 ahead 가 없어야 한다
```

Pages 반영 확인(2분 뒤): `curl -s https://jaefit.github.io/hanwha-ai-challenge/deck.html | grep -c '<section class="slide"'` → `12`.

---

## 자체 검토 (플랜 ↔ 스펙)

- §2 범위: 12장·라이트·임베드·이식 4종·코드 스트립·노트·10장 자리·exporter·tests·README — Task 1~6 에 전부 대응. 비범위(동결 파일·영상·fireworks·QR·캡처 생성)는 어떤 태스크도 건드리지 않는다
- §3 표 12장 ↔ Task 2 마크업 12장 1:1. 1장은 스펙 수정(2026-09-04)대로 큰 숫자
- §4 토큰 ↔ Task 2 `:root` 값 동일. 시리즈 2색 검증 결과를 Global Constraints 에 기록
- §5 임베드·폴백 ↔ Task 2 `watchEmbed`(onLine · 8초) + 마크업 `data-src`. geolocation 없음 = 테스트
- §6 스트립 ↔ Task 1 `code_strips()` + Task 2 `loadStrips()` + 테스트 `test_code_strips_match_source`
- §7 데이터 ↔ Task 1 산출 5개 + 기존 5개. `docs/data` 밖 = 기존 테스트 유지
- §8 테스트 ↔ Task 1(5) · Task 2(4 + 키 교체) · Task 5(등호) · Task 6(노트) = 신규 10, 기존 9 유지 → `test_deck.py` 19건
- §9 git ↔ Global Constraints (`--autostash`)
- §10 DoD 1·2·5·6 ↔ Task 6 Step 5·6·4·8, DoD 3·4 ↔ Task 6 Step 7(사람)
- 자리표시자 없음. 시그니처 일관: `fn(cv, d, slide, fig)` · `animate(cv, ms, draw)` · `label(..., font)` · `CHARTS.<key> = [json, fn]`
